# 开发需求

> 记录"印迹"做什么、做到什么程度、以后往哪走。改需求请更新本文件。

## 1. 目标与定位

一个轻巧、简洁、直观的**照片水印网页 App**，面向 iPhone（Safari）。给照片加上类似 OPPO / 徕卡的相机水印，第一版聚焦最简单的"白框黑字"样式。

## 2. 平台与形态

- **核心**:单文件网页 App(`watermark.html`),所有 UI 和逻辑都在里面。
- **唯一上线路径:PWA**。项目自带 `manifest.webmanifest` + 图标 + `sw.js`,**浏览器打开网址即可"安装到桌面 / 主屏"**,Win / 安卓 / iOS / macOS 全平台都能装,体验接近原生 App,**改一处全平台同步**。
- **已弃用的路线**(2026-05-28 v2.3.0 决策):
  - ~~安卓 Capacitor 打包 .apk~~ — 不再做。原因:维护两套发布太重,PWA 在安卓上体验已经足够;材料(签名 / 软著 / 商店审核)对个人非程序员负责人门槛太高。
  - ~~iOS Capacitor 打包 .ipa~~ — 不再做。原因:需要 Mac + 苹果开发者账号 $99/年。
- **保留可选**:Windows Tauri 出 `.exe` 安装包(给不想用浏览器的桌面用户)。优先级低于 PWA。
- **核心原则**:核心 `watermark.html` 是唯一真相源。所有"装上"方式都不动核心。详见 `docs/cross-platform.md`。

## 3. 核心功能（第一版，已完成）

- **选择照片**：从相册选，或直接拍。
- **26 种预设水印样式 + 自定义模板，一键切换**：

  **基础白款**:
  - **经典**(classic):底部白栏 + 黑字,最克制简洁。
  - **拍立得**(polaroid):四周白边,底部更厚,复古文艺。
  - **宝丽来完全款**(polaroidPlus):加厚拍立得 + 米黄底 + 暖色调,做旧。
  - **极简框**(minimalFrame):细白边 + 下方小字,近乎隐形。
  - **杂志浅版**(magazineLight):白底 + 黑字大标题。

  **深色款**:
  - **黑底白字**(darkBar):底栏深黑 + 白字,夜景照片更搭。
  - **电影黑边**(cinema):上下黑色横条 + 白字,16:9 letterbox 感。
  - **杂志封面**(magazine):照片满版 + 底部暗渐变 + 白色大标题,时尚杂志风。
  - **哈苏风**(hasselblad):深色底栏 + 橙色细条 + 橙字。

  **彩色 / 特殊款**:
  - **徕卡**(leica):底栏顶部一道粗徕卡红横条 + 自动模式分隔线变红。
  - **节日红**(festivalRed):红色边框 + 金色文字,过年/节庆。
  - **复古胶片**(filmStrip):左右两侧加胶片齿孔,米黄底,胶卷感。
  - **签到打卡**(checkIn):底栏带红色定位图标,旅行打卡风。
  - **大字日期**(bigDate):照片上压巨大年份数字,大字报感。
  - **极简右下角**(minimalCorner):不加底栏,右下角一行小字。

  **季节 / 节气款**(v2.7.1 新增,一套渲染四色):
  - **春·樱**(springCherry):粉色调底栏 + 细横线,春日感。
  - **夏·海**(summerSea):清爽蓝调,夏天/海边。
  - **秋·枫**(autumnMaple):暖橙调,秋意。
  - **冬·雪**(winterSnow):冷灰调,冬日/雪景。

  **相机品牌风**(v2.7.1 新增,深色底栏 + 品牌色细条 + 品牌色字):
  - **富士绿**(fujiFilm):墨绿底 + 富士绿。
  - **索尼橙**(sonyAlpha):黑底 + 索尼橙。
  - **尼康黄**(nikonGold):暖黑底 + 尼康黄。

  **玻璃质感款**(v2.7.1 新增,半透明磨砂浮在照片上,iPhone Safari 最佳):
  - **玻璃底栏**(glassBar):底部磨砂玻璃横栏 + 白字,液态玻璃感(主样式条直达)。
  - **玻璃卡片**(glassCard):底部居中浮一块圆角磨砂卡片装文字。

  **节日 / 纪念日款**(v2.7.1 新增):
  - **生日快乐**(birthday):暖色渐变底栏 + 彩色纸屑点 + 棕字。
  - **纪念日**(anniversary):白底栏 + 玫瑰色爱心 + 玫瑰字。

- **自定义模板**:点搜索弹窗里的「+ 新建我的模板」 → 选布局(底栏 / 四周白边 / 满版渐变)→ 调底色 / 文字色 / 强调色 → 起名保存。保存在浏览器 localStorage,本机持久;在搜索面板里能找到、能编辑、能删除。
- **两种文字模式，一键切换**（和样式正交，可任意组合）：
  - 手动输入：用户自己写一句话（默认预填当天日期）。
  - 自动读取：从照片 EXIF 读出**拍摄时间、设备型号、光圈、快门、ISO、焦段**，排成相机水印样式。
- **实时预览**：改文字 / 切模式 / 切样式即时更新。
- **「水印 / 滤镜」分页**（v3.0.0-beta.8；**v3.0.0-beta.11 起标签「调色」改叫「滤镜」**）：进编辑器后顶部有「水印 / 滤镜」两个标签，点哪个只露出哪一组操作——「水印」页放选水印 + 手动 / 自动文字，「滤镜」页放滤镜 + 调色盘 + 帮我配色 + 来点灵感。**只是把操作分两页看着更清爽，导出的成图照旧同时带水印 + 滤镜，效果一点没变**；每次打开新照片默认从「水印」页进。
- **保存**：点「保存到相册」直接调系统「存储图像」进相册（不支持时退化为下载 / 长按存图）；可选导出画质（标准 / 高清 / 无损 PNG）；JPEG 导出自动保留原图拍摄信息（EXIF）。
- **模板搜索**：右上角搜索图标点开，弹出输入框 + 模板名单，按名字 / 描述 / 标签实时筛选（如「红」「复古」「杂志」「暖」），点一下应用并关闭。
- **复制到剪贴板**（v2.8.0）：保存弹窗里「复制图片」一键把成图写进系统剪贴板（PNG），直接粘贴到聊天 / 备忘录 / 文档（浏览器支持 `ClipboardItem` 时才显示）。
- **记住上次选择**（v2.8.0）：上次用的水印样式、文字模式（自动 / 手动）、导出画质本机记住，下次打开自动还原，不用重选。
- **长按看原图**（v2.8.0）：编辑时长按预览图临时显示无水印原图，松手恢复，方便对比加水印前后。
- **自挑主样式条常用模板**（v2.8.1，**已于 v3.0.0-beta.9/beta.10 移除**）：曾在主界面顶部提供一排用户可自挑的快捷模板（常用栏 / segPins）。自 beta.9 起「水印」直接在编辑器「水印」页内嵌分类浏览、常用栏不再显示；beta.10 整套 segPins 代码与设置里的「重置常用栏」项一并删除。保留此条仅作历史记录。
- **独立设置页**（v2.9.0）：右上角齿轮图标打开，把所有可调项集中到一处，分六组：
  - **外观**：主题（跟随系统 / 浅色 / 深色）、**主题色**（v3.0.0-beta.5：6 个预设 + 取色盘自定义，只改 App 点缀色、不影响水印成图）、液态玻璃开关（从「更多模板」面板搬来）。
  - **打开照片时**：记住上次选择开关、默认文字模式（用上次的 / 手动 / 自动）、默认水印文字（今天日期 / 留空 / 固定一句）。
  - **自动模式显示哪些信息**：设备 / 时间 / 光圈 / 快门 / ISO / 焦段 六项各自可勾选，自动模式下只显示勾选的。
  - **照片与导出**：默认导出画质、导出尺寸上限（省流 1600 / 标准 2200 / 高清 3200，指最长边像素）、长按看原图开关。
  - **数据与隐私**：清空我的水印 / 恢复默认设置，附隐私说明。（v3.0.0-beta.10：原「重置常用栏」项随常用栏一并移除；「清空我的模板」措辞统一改为「清空我的自定义水印」。）
  - **关于**：版本号、重看新手引导。
  - 所有设置写入 localStorage `yinji.settings.v1`，本机持久。
- **深色模式**（v2.9.0）：设置「外观」里选「跟随系统 / 浅色 / 深色」。跟随系统时随 `prefers-color-scheme` 自动切换。**只影响 App 界面配色，不改水印照片本身的颜色**。
- **新手引导**（v2.9.0）：首次打开自动弹 4 张分步卡片（欢迎 + 本地不上传 → 选图 + 挑样式 → 手动 / 自动文字 → 保存 / 复制），看过本机记住不再弹；设置「关于」里可「重看新手引导」。
- **添加模板按钮 / 默认打开模板 / 文件名 / 新样式**（v2.9.1）：
  - 主样式条最前的「+」改为「＋ 添加模板」，一眼看懂是新建自己的水印款。
  - 设置「打开照片时」加「默认打开模板」下拉：指定每次开图默认套用的样式，或保持「用上次用的」。
  - 导出文件名带日期 + 样式名（如 `印迹_2026-05-31_经典.jpg`），相册 / 文件里好认好归类。
  - 新增 4 款预设（佳能红 / 毕业季 / 莫兰迪 / 节气·青），样式 26 → 30 款。
- **3.0 AI 接口预留**（v2.9.1）：代码层留出 `window.Yinji.ai` 扩展点 + 只读上下文（不含照片像素），2.x 不产生任何 AI 行为、不联网；并在设置加「AI · 即将推出」预告段。为 3.0 接入做准备，详见 `tech.md` 6.11。

## 4. UI 原则

简洁、直观为第一原则。主界面只暴露必要操作（选图 → 选模式 → 保存），不堆功能。具体视觉见 `design.md`。

## 5. 隐私

照片仅在本地处理，**不上传**到任何服务器。

## 6. 已知限制（第一版接受）

- 自动读取依赖照片自带 EXIF；截图、转发过的照片常丢失，读不到时自动切回手动输入并提示。
- 不含地点信息（GPS 转地名需额外服务，且常被系统抹除）。
- iPhone 的 HEIC 格式：拍摄信息读取已在 iPhone Safari 支持（v2.5.0）；非 Safari 浏览器仍可能解不开 HEIC 图像，会提示先转 JPEG。
- 实况照片（Live Photo）只能导出静态那一帧 + 水印，动态部分网页拿不到（浏览器只交付静态封面帧，硬限制）。
- 复制图片到剪贴板依赖浏览器 `ClipboardItem` 写图能力（iPhone Safari、桌面 Chrome / Edge 支持）；个别老浏览器不支持时按钮自动隐藏，仍可用「保存到相册 / 下载文件」。
- 深色模式只换 App 界面配色，不改水印照片输出（水印自带固定配色，按样式来）。「跟随系统」依赖浏览器 `prefers-color-scheme`，极老浏览器不支持时按浅色处理。

## 7. 后续路线图（待办）

- ✅ 已完成:PWA 部署到 GitHub Pages,网址 https://lysandrazyq.github.io/yinji/watermark.html
- ✅ 已完成(v2.5.0):HEIC 拍摄信息读取(EXIF 部分,iPhone Safari 完整工作)。HEIC 图像解码在 Chrome/Edge 上仍受限,需用户先转 JPEG。
- Windows .exe 打包(Tauri,可选,优先级低)。
- ✅ 已完成(v2.6.0):自定义模板的导入 / 导出(分享码,朋友间复制粘贴即可分享,完全离线生成)。
- ✅ 已完成(v2.7.0):导出升级——「保存到相册」按钮(Web Share「存储图像」)、画质三选一(标准 / 高清 / 无损 PNG)、JPEG 保留拍摄信息(EXIF 回写)。
- ✅ 已完成(v2.8.0):一键复制图片到剪贴板;记住上次的样式 / 模式 / 画质;长按预览看原图对比;玻璃磨砂重做(downscale-upscale,真机可见);顶部状态栏配色统一。
- ✅ 已完成(v2.8.1):主样式条快捷模板用户可自挑(常用栏,localStorage 记住);「复制图片」按钮位置修正(并入按钮行)。
- ✅ 已完成(v2.9.0):独立设置页(四组新设置:默认行为 / 水印内容 / 照片与导出 / 数据与隐私)+ 深色模式(跟随系统 / 浅 / 深)+ 首跑分步卡片新手引导。
- ✅ 已完成(v2.9.1):主样式条「+」文案化为「＋ 添加模板」;设置加「默认打开模板」(每次开图默认样式,或保持「用上次用的」);导出文件名带日期 + 样式名;新增 4 款预设(佳能红 / 毕业季 / 莫兰迪 / 节气·青,26 → 30 款);**预留 3.0 AI 接口**(`window.Yinji.ai` 扩展点 + 只读上下文,不含照片像素)+ 设置「AI · 即将推出」预告段。
- ✅ 已完成(v2.9.2):顶部常用栏默认改空(`segPins.v2`),首屏只剩一个大「添加模板」按钮、由用户自行钉选(在「更多模板」点「☆ 常用」);取消"至少留一个常用模板"限制(可清到 0);设置齿轮图标重画为对称 8 齿。
- ✅ 已完成(v2.10.0):新增 5 款预设(中秋·月 / 雾蓝 / 暮山 / 黛紫 / 暗夜蓝),样式 30 → 35 款;全部复用现有共享渲染器(drawSeasonStyle / drawBrandStyle),只加调色板再登记,零新绘制代码。
- ✅ 已完成(v2.11.0):PWA「有新版本」更新提示——上线新版后已装到主屏的用户会收到底部提示条 +「刷新」按钮、一键换新版(`sw.js` 改消息触发激活 + 页面 `updatefound`/`controllerchange` 检测,带防首装误刷新闸)。注意 SW"隔一版生效":本版上线后老用户需手动刷新一次拿到 v2.11.0,之后的新版才自动弹。
- ✅ 已完成(v2.12.0,**2.0 大版本收官**):① **调色 · 色调影调(Beta)**——8 款一键色调(原图 / 黑白 / 复古 / 暖阳 / 冷调 / 清新 / 高级灰 / 鲜艳),在 `getSize()` 单点注入(逐像素 `TONE_PARAMS`,非 `ctx.filter`,iOS 可靠),35 款样式 + 导出全继承、随成图保存;完整版(强度滑杆 / 更多胶片色)留 3.0。② **玻璃四周(glassFrame)** 新预设——四边磨砂 + 底栏文字,复用 `frostRect`,归「玻璃」分类。③ **「添加模板」→「选择模板」**——点顶部大按钮直接开「更多模板」面板挑样式(自建入口仍在面板内)。
- ✅ 已完成(v2.13.0):**调色盘**——把 v2.12.0 的一键色调(Beta)升级为**可手动微调**:一块 iPhone 16 Pro 风格的 2D 调色盘(横轴冷暖 / 纵轴品红–绿)+ 强度滑杆(调色浓淡,0 = 原片)+ 饱和滑杆 + 复位。在 8 款预设之上叠 `state.toneAdj` 微调层,经 `effectiveParams()` 合并后仍走 v2.12.0 的 `getSize()` 单点注入,36 款样式 + 导出全继承、随成图保存。**定位为 3.0「智能配色」的手动版地基**(AI 将来填同一套 `toneAdj`),详见 `docs/roadmap-3.0.md` A1。
- ✅ 已完成(v2.14.0,**滤镜系统**):① **「调色」统一改叫滤镜**——一档内置滤镜 = 一整套完成观感,调色盘成了「在某套滤镜上再手动微调」的图层。② **滤镜从 8 款扩到 20 款、分 6 类**(胶片 / 黑白 / 暖调 / 冷调 / 清新 / 氛围),新增 12 款只往 `TONE_LIST`/`TONE_PARAMS` 加数据,仍走 `getSize()` 单点注入,零逐样式改动。③ **🎲 随机一套**——一键随机「模板 + 滤镜」,用户无需自己挑。④ **存为我的滤镜 + 分享 / 导入**——把当前调色盘效果存成自定义滤镜(本机 `yinji.filters.v1`),可生成离线分享码、复制给朋友导入(与模板共用一套编解码,按 `type` 路由)。**定位为 3.0 滤镜体系的地基**,详见 `docs/roadmap-3.0.md` A1。
- ✅ 已完成(v3.0.0-beta.1,**3.0 首个 AI 功能**):**智能配色 A1(端上启发式)**——调色盘面板顶部加「✨ 帮我配色」按钮,点一下把当前照片缩到 48×48 采样(平均色 / 亮度 / 饱和),按规则算出一组 `toneAdj` 写进调色盘并立即重绘:偏冷回暖(回暖比压冷更积极、有封顶)、纠绿 / 品红偏色、灰则提饱和。**纯本地、不联网、不下载模型、照片不离开设备**;复用 v2.13.0 调色盘管线、渲染零改动,拨完属「未保存的微调」,可复位 / 存为滤镜。注册到 `window.Yinji.ai`(`available` 翻 true)、`getContext()` 增聚合 `colorStats`(非像素)。详见 `docs/roadmap-3.0.md` A1。
- ✅ 已完成(v3.0.0-beta.2,**界面紧凑化 + 动效弹性**):**纯 CSS、零 JS / 零渲染改动**——① 整体 UI 等比收紧(圆角 22→18px、头部 / 横幅 / 面板 padding 与字号一圈缩小、图标按钮 44→40px),让横幅(4:3)照片能一屏放下(竖幅 3:4 仍需轻滑);② 动效加「弹性」——新增 `--spring:cubic-bezier(.34,1.5,.56,1)` 缓动,入场(头部 / 面板 / 空状态)translateY 略微回弹、`yj-scaleIn` 改 3 段过冲(.93→1.018→1)、按钮 / 分段控件按压用同一 spring,手感更"弹";保留 `prefers-reduced-motion` 关动效。**只改外观尺寸与手感,功能 / 滤镜 / 导出全不变**。
- ✅ 已完成(v3.0.0-beta.3,**拼图基础版**):空状态新增「拼图」入口——一次选 2–4 张照片,合成一张方形大图(固定格子模板:2 张左右 / 上下、3 张竖排三 / 横排三 / 1 大 2 小、4 张田字格 / 竖排四 / 横排四),预览选好点「用这个拼」即当成一张普通照片接入编辑器。**核心:合成到离屏 canvas → `state.bitmap=canvas`**,故既有水印 / 滤镜 / 调色 / 导出管线零改动全继承(拼图无单一 EXIF → `state.exif=null`)。照片仅本地合成、不上传。定位为「自定义拼贴」的基础版,自由拖拽 / 调间距 / 边框留后续迭代。**(v3.0.0-beta.13 起拼图升为编辑器第三个标签「拼贴」,与「水印 / 滤镜」并列共存、可随时回改,见下方决策记录。)**
- ✅ 已完成(v3.0.0-beta.4,**水印扩充**):新增 6 款预设,样式 **36 → 42 款**——奶系柔色 4 款(抹茶 / 奶茶 / 藕粉 / 燕麦,共用 `drawSeasonStyle`)+ 经典胶片 2 款(银盐 = 黑白单色 / 柯达金 = 暖调,共用 `drawBrandStyle`);全部复用现有共享渲染器,只加调色板再登记(`TEMPLATES` / `DRAW_DISPATCH` / `CAT_IDS` 同步),**零新绘制代码**。奶系四款归「季节」分类、胶片两款归「品牌」分类。
- ✅ 已完成(v3.0.0-beta.5,**自定义主题色**):设置「外观」新增「主题色」——6 个预设(赤陶默认 / 海蓝 / 松绿 / 黛紫 / 胭脂 / 墨灰)+ 彩虹取色盘任意挑。改的是全局强调色 `--accent`(作为 inline style 写到 `body`,优先级盖过浅 / 深两套默认),`--accent-soft` 按明暗自动算出淡 / 深底色;`accent` 入 `yinji.settings.v1` 本机持久。**只改 App 界面点缀色(按钮 / 开关 / 选中高亮),不影响照片水印的成图效果**(画布只读 JS 端硬编码调色板、不读 CSS 变量)。
- ✅ 已完成(v3.0.0-beta.6,**拼图加文字 / 贴纸**):在 beta.3 拼图基础上,选好照片后可往拼图上**加文字 + emoji 贴纸**——点「＋ 文字」打字、点 emoji 贴纸(❤️ ⭐ ✨ 🌸 🌿 📷 🎀 ☀️ 🌈 👍)贴上,**手指直接拖**到任意位置;选中后可「小一点 / 大一点 / 改文字 / 颜色 / 删除」(改文字、颜色只对文字有效)。文字带浅色描边,压在深色照片上也看得清。点「用这个拼」时**文字 / 贴纸一起烤进 1500² 大图**,随后照常加水印 / 滤镜 / 导出。实现:加一层 `collageDecos`(归一化坐标 + 高度占比,预览 600 与成图 1500 等比一致),`drawCollageInto` 末尾叠 deco,Pointer Events 拖动。贴纸用系统自带 emoji 字形,**零外部资源、纯本地**。即 6/4「画图功能 × 自定义拼贴结合」南心确认范围(拼图上加文字 / 贴纸,不做自由手绘)。
- ✅ 已完成(v3.0.0-beta.7,**3.0 第二个 AI 功能 · 智能文案灵感 A3 端上规则版**):「手动输入」文字框下加「✨ 来点灵感」按钮,点一下读这张照片的**拍摄时间 / 季节 / 整体色调**,用规则 + 词库套句式生成 2~3 个文案候选(像「秋日暮色」「暮色 · 浅焦」「温柔的光」),渲染成可点 chip——点中直接填进文字框、可再手改,再点一次换一批。线索来自 `exifClues(state.exif)`(从 EXIF 拍摄时间解析时段 / 季节 + 设备 + 大光圈 / 焦段)+ A1 的 `state.toneAdj` 冷暖 / 明暗倾向。**纯端上、不联网、不上传照片、不下载模型**,断网可用。已注册为 `window.Yinji.ai` 第二个功能(`id:"caption"`,`ai.list()` 含 smartColor + caption);`getContext()` 增只读字段 `hasExif/timeOfDay/season/device/lens`,延续「快照不含照片像素、不含原始 GPS」铁律。定位诚实为"灵感模板"(真正的自然语言生成是联网 B1,默认关)。详见 `docs/roadmap-3.0.md` A3。**(v3.0.0-beta.14 起联网 B1 已落地——BYOK、默认关、只发文字、失败回退本端上规则版,见下方决策记录。)**
- ✅ 已完成(v3.0.0-beta.8,**编辑器「水印 / 调色」分页 + 代码注释清理 + UI 简洁化**):① **水印 / 调色分页**——进编辑器后顶部有「水印 / 调色」两个标签,点哪个只露出哪一组操作(「水印」= 选模板 + 手动 / 自动文字;「调色」= 滤镜 + 调色盘 + 帮我配色 + 来点灵感)。**只是把操作分两页看着更清爽——导出的成图照旧同时带水印 + 调色,效果一点没变**(`setEditTab` 只切 DOM 显隐、绝不动渲染管线,实测切「调色」页导出同一像素逐位一致);每次打开新照片默认从「水印」页进。② **代码注释清理**——清掉函数体里 448 条解释性注释、保留 45 条结构横幅。**⚠️ 清的是代码注释(程序员才看的绿字),不是删设置、不是删功能**:设置模块(齿轮 + 外观 / 打开照片时 / 自动信息 / 照片与导出 / 数据与隐私 / 关于六大分区)与 App 所有用法、导出全部完整保留——以「去掉所有注释 + 空白」的代码骨架逐字节比对,新旧文件完全相同(只动注释)。③ **UI 简洁化**——分页后界面更干净,当前标签用主题色点亮一眼看清在哪页。仍单文件 + 纯本地、不联网、不上传照片。
- ✅ 已完成(v3.0.0-beta.9,**水印模板「像滤镜一样」内嵌浏览 + 文案灵感升级**):① **水印模板区照搬滤镜区**——进编辑器「水印」页,以前只有一个「选择模板」按钮(还要再开「更多模板」弹窗才看得到模板),现在改成和「滤镜」页一样的内嵌浏览:一排**分类**(全部 / 经典 / 深色 / 彩色 / 季节 / 品牌 / 玻璃 / 节日 / 我的)+ 一排**模板 chips**(点一下直接套用、当前高亮),自定义模板收在「我的」分类、新建 / 导入后自动跳进去;底部一排动作(＋ 新建我的模板 / 导入模板 / 分享 / 删除,后两个仅选中自定义模板时显示)。**全程复用滤镜区现成 CSS、零新增样式**,故视觉与滤镜区一致。因模板已能在「水印」页直接浏览,**删掉头部「更多模板」放大镜按钮**(顶部只剩齿轮);旧「更多模板」弹窗保留但休眠(已无入口,后续清理)。② **文案灵感升级**——把 beta.7「按固定句式各产一句」改成 **15 个词库 + 洗牌去重取 4**(情境词:季节 / 时段 / 暖冷 / 明暗 / 浓淡 / 光圈 - 镜头 / 设备 + 通用词:诗意 / 日常 / 英文 / 日期),情境优先、通用兜底,无 EXIF / 无照片也有 4 条,每点一次重洗(换一批),仍纯端上不联网。**仅把"挑模板 / 给文案"的交互变顺手,导出的成图效果零变化**;仍单文件 + 纯本地、不联网、不上传照片。⚠️ 已知陈旧项:设置「重置主样式条常用栏」现已无可见效果(主样式条不再显示在「水印」页),待清理(#144 周边)。
- **2.0 已收官,后续主线转向 3.0 AI**(零星 2.x 小改仍可按需发补丁,详见 `docs/roadmap.md`)。
- **下一个大版本 3.0.0:以 AI 能力为主线**(待规划,详见 `docs/roadmap.md`「3.0 及以后」)。接口已在 v2.9.1 预留;隐私铁律不变——**照片绝不静默上传,端上处理优先,联网 AI 默认关闭且每次单独征得同意,且 AI 绝不作为核心加水印功能的前置条件**(详见 `tech.md` 6.11)。
- 地点信息(GPS → 地名)。
- 继续扩充预设(已加中秋·月 / 雾蓝 / 暮山 / 黛紫 / 暗夜蓝 / 节气·青 / 毕业季 / 莫兰迪 / 佳能红;可再加对联 / 婚礼 / 宝宝成长第 N 天等,详见 `roadmap.md` 3.1)。
- HEIC 图像解码在非 Safari 浏览器(长期,需要 libheif WASM,体积代价大,暂缓)。

## 8. 决策记录（为什么这么定）

- **核心永远是网页**:负责人非程序员,网页零安装、零成本、改一处全平台同步。原生 App 只做"套壳",不是另起炉灶。
- **样式与文字模式正交**:样式控制外观,文字模式(手动 / 自动)控制内容,互不影响,组合更灵活。
- **自定义模板派生**:自定义模板不是从零写,而是基于"布局类型"(底栏 / 白边 / 渐变) + 颜色覆盖。这样实现简单,用户也好上手。
- **默认模式**:有 EXIF 时默认"自动读取"(更出彩),无 EXIF 时默认"手动输入"。
- **平台接口而非平台条件分支**:`window.Yinji.platform.*` 抽象保存 / 选图 / 存储等动作,各平台只是注入不同实现,核心代码不变。
- **设置集中、键分散**(v2.9.0):新增设置统一收进一个 `yinji.settings.v1` 对象(以后加开关便宜),但**不合并**已有的 `yinji.glass` / `yinji.prefs.v1` / `yinji.segPins.v1` 三个老键——各自独立、避免迁移回归。读取用惰性 `ensureSettings()` + "存储值合并到默认值之上",对部分 / 损坏 / 旧数据健壮。
- **深色模式只换界面、不碰水印**(v2.9.0):深色是 `body[data-theme]` 上的一层 UI 调色板覆盖,canvas 水印输出完全不受影响(水印颜色按样式固定)。"跟随系统"在 JS 里用 `matchMedia` 解析成显式浅 / 深,CSS 只认 `light/dark`。
- **新手引导首跑一次**(v2.9.0):用 `yinji.onboarded.v1` 标记是否看过,首跑自动弹、看过不再扰;设置里可重看。分步卡片(而非一次性长文)更轻、可跳过。
- **3.0 AI 接口提前预留**(v2.9.1):趁 2.x 收尾把扩展点定下来,避免 3.0 临时改核心结构。做法——在 `window.Yinji` 下加 `ai` 命名空间(与既有 `platform` 平级,`register/list/run`)+ `getContext()` 只读快照;快照**只给元信息(版本 / 有无照片 / 样式 / 模式 / 文字),绝不含照片像素**。2.x 不注册任何 AI 提供方,`run` 是空操作、不联网、零行为变化。隐私底线写进 `tech.md` 6.11,3.0 据此实现不得漂移。
- **新预设走"调色板 + 共享渲染器"**(v2.9.1):4 款新样式(佳能红 / 毕业季 / 莫兰迪 / 节气·青)不写新绘制代码,只给两个已验证的共享渲染器(`drawSeasonStyle` / `drawBrandStyle`)加调色板再登记,改动小、回归风险低。
- **常用栏从"预置"改"自挑"**(v2.9.2):顶部样式条默认空,首屏只留一个醒目的大「添加模板」按钮,把"放哪些快捷样式"完全交给用户(在「更多模板」点「☆ 常用」)。理由:预置一排样式对新人是噪音、也不暗示"可换",而空栏 + 一个大按钮更聚焦"先建 / 先挑";要快捷栏的人自己钉,所见即所选。代价:存储键 `segPins.v1→v2` 让新默认对老用户立即生效,旧的自动预置会清一次(秒级可重钉,自定义模板 / 照片不受影响)。配套**取消"至少留一个"守卫**,使"默认空"与"可清空到 0"自洽。
- **调色在 `getSize()` 单点注入、用像素而非滤镜**(v2.12.0):把"调一次色 → 缓存离屏 canvas → `getSize()` 返回它当 `bmp`"放在所有绘制 / 导出的上游,使 35 款样式 + 导出零改动全继承调色,而非逐样式接调色参数。颜色变换走 `getImageData`/`putImageData` 逐像素算——因为 iOS Safari 对 canvas 不应用 `ctx.filter` 的颜色类滤镜(同玻璃磨砂踩过的坑)。Beta 只给现成色调,强度可调等留 3.0,避免 2.0 收官版背太重的新 UI。
- **「添加模板」改「选择模板」**(v2.12.0):顶部大按钮原是"直接进自建编辑器",但对多数用户更自然的动作是"先看看有哪些现成样式"。故改名 + 改为打开「更多模板」面板;自建入口不删,移到面板内的「＋ 新建我的模板」,兼顾两类用户。
- **2.0 收官、调色完整版留 3.0**(v2.12.0):2.x 主打"本地、零外链、稳"的样式与易用性打磨,到 v2.12.0 基本齐活,作为大版本收官。调色的进阶能力(强度 / 更多胶片色 / AI 荐色)天然与 3.0 的"端上 AI"同源,合并到 3.0 一起做,避免 2.x 尾期再加重前端。
- **调色盘做成"预设之上的微调层",而非推倒重来**(v2.13.0):不另写一套调色,而是在 v2.12.0 的 8 款预设之上叠一层 `state.toneAdj{temp,tint,sat,intensity}`,经 `effectiveParams()` 合并后仍走同一个 `getSize()` 单点注入——既复用了"36 款样式 + 导出零改动全继承"的成熟管线,又让预设与手动盘可叠可单用。强度滑杆用"原片 ↔ 调色结果"线性混合(K=0 即原图)实现,直观且天然支持"轻调"。**关键决策:这块手动盘刻意做成 3.0「智能配色」的地基**——AI 自动配色将来写入的就是同一个 `toneAdj`,用户随时能接手微调,省得 3.0 再造一套调色 UI(详见 `roadmap-3.0.md` A1)。±100 拖动映射成 ±40 实际偏移,是为手感不过冲。
- **滤镜采用"一档 = 一整套观感"(Model B),内置选中即重置微调**(v2.14.0):把调色统一叫"滤镜",并定下交互模型——点内置滤镜 = 套用一整套完成态(同时把调色盘 `toneAdj` 归中性、清自定义选中);一旦用户动了调色盘 / 滑杆 / 复位,当前观感就降级为「未保存的微调」(`clearFilterSel()`),可再「＋ 存为滤镜」收藏。理由:让"滤镜"语义干净——选它就是它,不残留上一套的手动偏移;手动微调是独立动作,想留就显式存。**新增 12 款预设刻意只往 `TONE_LIST`/`TONE_PARAMS` 加数据**,直接被既有 `effectiveParams()`→`gradedBitmap()` 单点注入管线消费,零逐样式 / 逐 draw 改动,延续 v2.12.0 起的"调一次色全继承"架构。
- **自定义滤镜与模板共用一套分享码、按 `type` 路由**(v2.14.0):自定义滤镜 = `{name,base(底色 tone),adj(调色盘值)}`,编码沿用模板的 `SHARE_PREFIX="YINJI1:"` + UTF-8 安全 base64,JSON 里打 `type:"yinji-filter"`;`decodeShareRaw()` 返回整个对象,`doImport()` 按 `type==="yinji-filter"∥有 flt 字段` 决定走滤镜还是模板导入。理由:复用已验证的离线编解码 / 弹窗 UI,不为滤镜再造一套;`type` 字段让一个输入框同时认两种码,体验统一。自定义滤镜单独存 `yinji.filters.v1`(与模板键分开,避免迁移回归),全程离线、不上传。
- **🎲 随机一套同时随机"模板 + 滤镜"**(v2.14.0):应负责人"不用用户自己挑"的诉求,一键随机一个水印模板 + 一款非原图内置滤镜并立即套用,toast 报出组合名。理由:降低"选择困难",让用户一键得到一个完整成品再决定是否微调;随机时把 `toneAdj` 归中性、`filterId` 清空,确保拿到的是干净的一整套而非叠在旧微调上。
- **「随机一套」全局化 +「新建我的滤镜」常驻显示**(v3.0.0-beta.12):南心要"随机做成全局"——把 🎲 随机一套从滤镜页内部移到编辑器「水印 / 滤镜」标签栏右侧,**任意标签都能一键随机**(功能不变,仍随机一套水印 + 滤镜)。另修一处:「＋ 新建我的滤镜」原来只有"已调过效果"(`canSave = hasGrade() && !filterId`)时才显示,南心停在「原图」就找不到("存为滤镜在哪里"),改成与「水印」页「＋ 新建我的水印」一样**常驻显示**,没效果时点它由 `openFilterSave()` 给 toast 引导。理由:① 随机是高频"先给个完整成品"的动作,不该埋在某一个标签里;② 两个"新建我的…"按钮的叫法与可见性对齐,降低用户困惑(同一动作两处行为一致)。
- **3.0 第一个 AI 功能选"端上智能配色 A1"打头**(v3.0.0-beta.1):3.0 主打 AI,但首发刻意选最稳的一条——A1 纯端上启发式(无神经网络 / 无模型下载 / 无联网),直接复用 v2.13.0 调色盘的 `toneAdj` 管线,"把手动拨盘换成自动拨盘",渲染零改动。规则上**回暖比压冷更积极且都封顶**(`warmth<0` 系数 0.8、`warmth>0` 系数 0.35,clamp 到 [-22,35]),既贴合"印迹"暖调气质,又避免把本来暖的照片洗白或过度反向。坚持"建议而非强加":拨完是「未保存的微调」,可一键复位、也可存成滤镜。`getContext()` 只加**聚合 `colorStats`(平均色 / 亮度 / 冷暖,四舍五入、不可还原原图)**,延续"快照绝不含照片像素"的铁律。理由:用最小风险验证"AI 接入位 + 端上推断 + 调色盘地基"整条链路,为后续 A2 完整调色 / A3 文案灵感铺路。
- **UI 瘦身走"等比收紧"而非"重排布局"**(v3.0.0-beta.2):手机一屏放不下的诉求,选择把现有元素整体缩小一圈(圆角 / padding / 字号 / 图标按钮等比例下调),而非改信息架构或折叠面板。理由:等比收紧风险最低、视觉关系不变、零功能回归;重排(如面板分页 / 折叠)会改交互与可发现性,留作后续按需再议。落地后**横幅(4:3)可一屏放下,竖幅(3:4)因控件面板较高仍需轻滑**——这是刻意取舍(不为塞下竖幅而牺牲控件可见性 / 给预览强行封高);若以后要竖幅也一屏,再单独评估"预览封高 / 控件折叠"。动效统一用单一 `--spring` token(入场回弹 + 按压),`yj-scaleIn` 配 `--ease`(非 spring)避免双重过冲,既增手感又不滥用,且全程尊重 `prefers-reduced-motion`。
- **拼图走"合成即照片"、先固定格子再谈自由**(v3.0.0-beta.3):拼图刻意不碰渲染 / 导出管线,而是把 2–4 张照片画进一张离屏 canvas、令 `state.bitmap` 指向它——因为 `getSize()` 读 `width||naturalWidth`、渲染一律 `drawImage(bmp)`,故"拼好的图"对下游就是一张普通照片,水印 / 滤镜 / 调色 / 导出全继承,零新管线、零回归。布局先用归一化 `{x,y,w,h}` 的**固定格子模板**(每个张数给 2~3 种),而非一上来做可拖拽自由拼贴——理由:固定格子实现稳、覆盖多数"几张拼一张"诉求、当天即可落地;自由拼贴(拖动 / 缩放 / 调间距 / 边框)交互重、需独立评估,留作 beta.3 之后的迭代。合成用 cover 填充(`Math.max` 缩放 + 居中裁切)保证每格填满不变形;白底 + gap(≈size×1.2%) + 圆角(gap×1.4)是 beta.3 的固定观感(后续再开放可调)。拼图无单一来源,`state.exif=null`、导出不回写拍摄信息(本就是多张合成)。开局即把首个布局高亮(先 `renderCollagePreview()` 落定 `collageLayoutId` 再渲染选项),避免"预览已是布局①但芯片无高亮"的割裂感。
- **拼图从"一次性弹窗"升为"与水印 / 滤镜并列的常驻标签"**(v3.0.0-beta.13):南心要"做水印 / 滤镜时也能拼图",并确认"三个标签随便切、互不影响"。beta.3/beta.6 的拼图是一次性弹窗(点「用这个拼」一锤定音烤进 `state.bitmap`、拼图状态随即丢弃),无法回改。这版选 **Design A**——拼图状态(`collagePhotos` / `collageLayoutId` / `collageDecos`)全程驻留,做成编辑器第三个标签「拼贴」;任何拼贴改动都经 `applyCollageToMain()` **实时重合成**到同一离屏 `collageCanvas` 并写回 `state.bitmap`。因水印 / 滤镜本就叠在 `state.bitmap` 之上(beta.3 起"合成即照片"架构),三者天然共存、最终一张图全叠上。**没选**"标签里再开旧弹窗"的省事做法——那仍是一次性、不满足"共存 + 随时回改"。**关键踩坑**:`gradedBitmap()` 影调缓存以"同一对象引用"为键,复用同一 `collageCanvas` 只改像素时**必须** `invalidateGrade()`,否则滤镜拿陈旧缓存。性能:拖动只刷 600 预览、松手才 commit 到 1500² 大图。退役旧 `#collageOverlay`(删 HTML + 三个 var / 监听 + `closeCollage()`,防重复 id / 悬空引用)。**AI 文案 B1 按南心"先拼贴、再做 AI"推迟下一版。**
- **拼图入口并进主选图卡片、按张数自动分流**(v3.0.0-beta.15):南心"我要在这个界面就能一次最多选择 9 张图片进行拼图,删掉底下的"。给了两条路(并进主入口 / 保留独立入口),南心选**并进主入口**。落地:首页「选择照片 · 拍照」的 `#fileInput` 加 `multiple`,`change` 按张数分流——选 **1 张**走原 `loadFile()` 单张编辑、选 **2–9 张**走 `startCollageFromFiles()` 自动拼图;删掉首页底部那个独立的「拼贴」按钮(`#collageEntry` + CSS + var + 监听),抽出共享函数 `startCollageFromFiles()` 同时供主入口与编辑器内「换照片」复用。`COLLAGE_LAYOUTS` 布局表从 2/3/4 扩到 **2–9 张**(每布局 `cells.length` 等于照片数)。理由:① 非程序员心智里"选照片"就是一个入口,把"几张"留给 App 判断比让用户先决定"单张还是拼图"更自然;② 删掉重复入口让首页更聚焦。**刻意不动**渲染 / 导出管线——拼图仍烤进离屏 `collageCanvas`(1500²)→ `state.bitmap`,水印 / 滤镜 / 导出全继承(沿用 beta.3 起"合成即照片"架构),零回归。这是 **3.0.0 beta 收口第一步**;南心三件事的第 2 件「拼贴标签→贴纸」(deco 引擎泛化到任意照片)体量更大,另起 **beta.16** 做。`.stage-hint`「长按图片可看原图对比」仍按南心要求保留。
- **文字颜色自适应只改"裸字压照片"的款 / 按样式记忆用 recall 标志隔离**(v3.2.0):南心选「文字颜色自动黑白」+「按样式记忆调色」+"继续做",一版做两件。① **文字颜色自适应(§3.2)取舍**:排查后发现唯一受益款是 `minimalCorner`(裸字 + 阴影直接压照片)——`bigDate`/`magazine` 已有暗渐变保证可读、其余款字落在底栏 / 白框(设计色),故**只改 minimalCorner**(按 §3.2 注释"纯白 / 纯黑栏本就对比足够、可不参与";不为已可读的款瞎动 = 降回归)。`bgLumaAt` 读当前画布(已绘照片 + 滤镜)落字区均亮度,亮底深字 + 浅光晕 / 暗底白字 + 深阴影。**不做"自动 / 手动开关"**(§3.2 原提的开关)——自动即最优、加开关是多余 UI(且南心一贯"少灰字少设置")。② **按样式记忆调色(A2 之一)取舍**:核心难点是 `setStyle` 被随机 / 导入 / 启动 / 用户点击**共用**——若在 `setStyle` 内无条件恢复记忆,会**覆盖随机的随机色、覆盖导入 / 启动要还原的色**。故给 `setStyle(style, recall)` 加 **recall 标志**,**仅"用户点样式 chip"传 true** 触发恢复,其余调用路径(randomLook / import / startup / 删除回退)用不带 recall 的 `setStyle(id)` → 完全隔离、不受影响(浏览器实测随机仍设新随机滤镜不被覆盖)。记忆经 `savePrefs` **捎带**累积(所有调色改动已调 savePrefs,零额外挂钩),新键 `yinji.styleColors.v1` 与其它键分开(避免迁移回归)。两件均纯本地 + 零外链,浏览器逐项验证。
- **拍摄地点只做"纯本地的安全那半步":读 GPS 坐标 + 手填地名,默认关**(v3.1.0):南心"继续开发后续版本",按路线图 §3.5 落地"拍摄地点"。**关键取舍**:① **只做坐标 + 手填,不做 GPS→中文地名**——坐标转地名要联网查地图服务,碰隐私红线(把坐标发出去),按铁律留到 4.x 的"可选 + 授权"联网功能;这版纯本地、零联网。② **默认关(`autoFields.location=false`)**——GPS 是最敏感信息,绝不默认把位置印上去;用户在设置里主动开才生效(铁律:别默认把家庭住址水印上去)。③ **手填地名优先于坐标**——坐标(31.24°N…)难看,给个「地点」手填框让用户写"外滩"这类好名字,留空才回落坐标。④ **地点骑在既有信息行 `info.params` 上**(`buildInfo` 末尾 push)——而非给 42 个 draw 函数各加地点绘制;因绝大多数水印款已绘制 params 行(f/快门/ISO/焦段),地点零改 draw 即随之显示,极简款本不显示 params 故也不显示地点(合理且零回归)。⑤ **GPS 解析自己写**(EXIF GPS IFD `0x8825` → 度分秒 RATIONAL → 十进制、按 N/S/E/W 取负)——纯本地、不引任何库,守零外链;JPEG+HEIC 共用既有 `readTiff` 两路都支持。⑥ **不加灰色提示文字**(按南心一贯"去灰字"偏好,仅用 placeholder 说明)。Node 逐位验证 GPS 解析(31°14′24″N→31.24)+ 浏览器验证(默认关、toggle 持久化、零报错)。**路线图 §3.5 完成**。
- **访问统计走"运行时异步 + 本地不计 + 只数访问不发照片",接百度统计**(v3.0.1):南心要「能看到有多少人用了、只有我后台能看」。**先讲清前提**:跨设备数人头**必须靠一个外部聚合器**——本 App 无后端、纯本地,localStorage 只能数单台,所以这是**第一个对外发信号**的功能,会在"纯本地零对外"上破一个小口子(**只发匿名访问信号、绝不发照片**)。给南心三条路(隐私友好轻量 / 自建国内云 / 换带分析的部署平台),她选**隐私友好轻量**。**选型踩坑**:① 先试 GoatCounter(隐私友好、无 cookie)——**国内访问不到(被墙,ERR_CONNECTION_CLOSED)**,她打不开 = 访客也连不上 → 会大量漏数,弃;② 51.la 误进"移动应用(Android/iOS)统计"产品(appkey/scheme/applinks),非网页用;③ 最终选**百度统计**(国内最稳、专做网站,新增网站给 `hm.js` 代码)。**红线处理**:统计脚本**不是静态 `<script src>`,而是 `loadAnalytics()` 在 `load` 后 `setTimeout` 运行时异步注入**——不阻塞首屏、不触发"白屏老坑",与"零外链(加载期)"红线的**本意(防加载期阻塞)一致**(同 AI 联网 fetch 一个性质:运行时、非阻塞)。`localhost`/`127.0.0.1`/`file:` 守卫 → **本地与预览不计数**(免污染统计);`try/catch` 静默 → 断网 / 失败不影响 App;SW 对跨域请求 `return` → hm.baidu.com 不进缓存、不扰离线。站点 ID 是**公开标识(写在每个访客页面、非密钥)**故 Claude 写入(区别于 AI Key 那种秘钥永不代填)。**隐私如实告知**:百度统计**确会用 cookie / 收集访客信息**,故 `.privacy-note` 补一句老实话(可能用 cookie、只记匿名访问量、不涉及照片),**不夸大隐私**;但**照片只在本机处理、绝不上传**这条核心承诺**一字未动**且前置强调。**为何接受这个隐私折损**:南心明确要"能数到人"且选了"国内能用、省事"那一档,国内合规可用的统计(百度/51.la)都不如国外隐私工具干净,这是她知情后的取舍;若日后她要更干净,可换"自建计数(数据全归己、第三方不经手)"。属 v3.0 之上的独立小补丁,不影响任何现有功能。
- **beta → 正式版走"稳定收口、不塞新功能"**(v3.0.0 正式版):南心"规划正式版开发"→ 经 AskUserQuestion 给三条路(直接收口 / 加小功能再发 / 先做图四五),她选**直接稳定收口转正**,且**已自行真机验证 beta.18**。据此正式版**不加新功能**,只做三件收口事:① **清死代码 `#searchOverlay`**——beta.9 删入口后整套"更多模板"弹窗不可达、休眠至今,本版彻底退役。**取舍**:虽是"稳定优先"阶段,但确认这是**行为等价**的纯删除(removed 代码全不可达;3 个活函数 `deleteCustomTemplate`/`shareCustomTemplate`/`importTemplateObj` 里删的都是对已不可达弹窗的死调用,删前删后行为一致),且浏览器逐条回归四条活路径(新建 / 导入 / 分享 / 删除我的水印)确认无碍,故做;**保留** orphan CSS(无命中、零影响,删它反有误删共享规则风险)。② **横屏左右安全区**——补 `viewport-fit=cover` 缺的 `env(safe-area-inset-left/right)`,竖屏 inset=0 逐像素不变;**评估后不做** Dynamic Type(需 px→rem 大改、不属收口)与"横屏重排"(居中 480px 窄列已天然适配,不为非问题硬造改动)。③ **版本号转正** beta.18 → `v3.0.0`(去 `-beta`)+ sw.js 缓存名升 + 全量回归自检。**为何不在正式版加小功能**:南心已真机验证 beta.18,正式版越接近"她验证过的代码"越稳;拍摄地点 / 文字颜色自适应等加分项留 v3.0.x / v3.1 按需排,不在收口期引入未验证 delta。这版 JS 由 161230 → 156760 字符(少约 4.5KB);浏览器全量回归(版本 / 4 AI / 33 滤镜 / 贴纸 / 拼图 / 自定义水印四路 / 导出 / 安全区)全过、零报错。**v3.0.0 正式版就绪待部署**。
- **第三个端上 AI 选「A4 场景识别 → 推荐样式」收尾 Phase A**(v3.0.0-beta.18):南心三件事第 3 件「完成管家更新内容」的最后一项 = 管家 #152「再加一个 AI 功能(放最后做)」。**功能开放但有 `roadmap-3.0.md` Phase A 作施工图**,故不臆造、按规划取 **A4 场景识别荐样式**(A1 智能配色 beta.1 / A3 文案灵感 beta.7 之后,A4 是 Phase A 端上能力最后一块;A2 完整调色的"更多滤镜"部分已在 beta.17 落地)。**为何 A4 而非别的**:① 它是文档明列的下一项、与已有两个 AI(配色 / 文案)正交——A4 管"挑哪款水印样式",是新的用户可见能力,比继续打磨调色(A2)更像"再加一个 AI 功能";② 纯端上启发式天然满足隐私铁律,零联网风险,适合放在收口位稳稳交付。**实现守铁律四条**:只用聚合色彩占比(48×48 缩图采样的偏绿 / 偏蓝 / 亮度 / 冷暖,**不留逐像素、不可还原**)+ EXIF 派生(时段 / 季节 / 光圈 / 焦段),**不联网、不识图、不下模型、不上传照片、不读原始 GPS**——`roadmap-3.0.md` A4 明确"端上启发式优先,真识图要 ML 运行时会破零外链红线,故只做启发式版"。**规则粗判 5 类**(night / landscape / food / portrait / daily)→ 映射到既有内置样式 id(`SCENE_STYLES`),有季节再 unshift 当季样式,渲染成可点 chip,点击走现成 `setStyle()`。注册为 `window.Yinji.ai` 第 4 个 feature(`id:"scene"`,`onDevice:true`),复用 v2.9.1 扩展点 + A3 的"采样 / 规则 / chip"模式与 `.cap-spark`/`.cap-list`/`.cap-chip` 类(零新 CSS、零核心结构改动);`getContext()` 加只读 `scene` 字段(仍不含像素 / 原始 GPS)。**AI 绝不前置**:不点按钮,手动分类挑样式照旧(铁律第 4 条)。浏览器预览验证 5 种纯色场景分类逐一命中、点 chip 套样式、零报错。**至此 Phase A 三个端上 AI(配色 / 文案 / 荐样式)+ B1 联网文案齐活,v3.0.0 beta 全部开发完成**;后续 Phase B 增强 / A2 按样式记忆调色 / 拼图自由拼贴待南心定。
- **更多滤镜走"纯数据扩表、零新绘制"**(v3.0.0-beta.17):南心三件事第 3 件「完成管家更新内容」含 #151「增加更多滤镜预设」。延续 v2.12.0 起"调一次色 → `getSize()` 单点注入 → 全样式 + 导出零改动全继承"的架构,新增 12 款滤镜(20 → 32)**只往 `TONE_LIST`(chip 元数据)+ `TONE_PARAMS`(6 参:sat/con/bri/temp/tint/lift)两张表加数据**,不写任何新绘制 / 新管线代码——`applyTone` 逐像素变换、`effectiveParams`/`gradedBitmap` 缓存、chip 渲染(`TONE_LIST.forEach`)、分类过滤(`t.cat`)、🎲 随机池(`TONE_LIST` 遍历)、自定义滤镜 `base` 校验全部自动纳入新款,既有 `FILTER_CATEGORIES` 六类(胶片 / 黑白 / 暖色 / 冷色 / 清新 / 风格)不变,每类 +2 均匀铺开。**关键手法**:`applyTone` 的运算顺序是"先按 `sat` 去饱和 → 再叠 `temp/tint` 加色",故 `sat:0` + `temp/tint` 偏移**零额外代码**即得单色调——`bwSepia`(棕调,暖偏移)、`bwCool`(冷黑白,冷偏移)就是这么来的。新增 12 款参数全部落在既有 20 款的取值区间内(无越界、无极端值),保证观感一致、可叠调色盘微调。理由:① 滤镜是高频且低风险的"加法",数据扩表是这套架构最省、最稳的扩展方式,零回归;② "更多滤镜"是管家明确列项,先把确定性高的做掉,把唯一的大件 #152「再加一个 AI」留到最后。浏览器预览像素级验证(注入纯色 photo 采样、`#toneSeg` 恰 33 chip、6 款手算逐位吻合)确认数据正确。这是 **3.0.0 beta 收口倒数第二步**;只剩 #152 即 v3.0.0 beta 全部开发完成。
- **编辑器第三标签从「拼贴」改「贴纸」、拼图能力保留、贴纸做成独立图层**(v3.0.0-beta.16):南心"拼贴改成贴纸 功能就是添加贴纸,没有拼图了",管家口径"贴纸贴任意照片、可拖动、emoji + 手绘 + 文字"。因 beta.15 已把**拼图入口**并进主选图卡片(多选 2–9 张),编辑器第三标签的"拼贴"语义就空出来了——本版把它改成「贴纸」,服务"给任意一张照片(单张或拼好的)贴东西"。**关键设计:贴纸不复用 beta.6/13 那套绑死在拼图预览上的 `collageDecos`,而是新开一个独立图层** `state.stickers`(每项 `{type:'text'|'emoji'|'doodle', x, y, content, size, color}`,坐标归一 0–1、尺寸相对 `Math.min(canvas.w,canvas.h)`),`drawStickerLayer()` 在 `draw()` 末尾**永远最后画**在主 `#canvas` 上。理由:① 贴纸要能贴"任意照片"(不止拼图),挂在主画布层比挂在拼图预览层更通用;② 滤镜 / 水印 / 拼图本就先后叠在 `state.bitmap`/主画布上,贴纸作为最上层,四者天然共存、一张图全叠、零管线改动(沿用 beta.3 起"合成即照片"+ beta.8"只切 DOM 不动渲染"架构)。**拼图能力没删**:主入口多选仍拼图,拼好后落「贴纸」标签、版式行 `#stickerLayoutRow`(原 `#collageLayouts`)≥2 张仍显示、可继续换版式——"没有拼图了"是指**这个标签不再是拼图操作台**,而非全局删拼图(已与南心 beta.15 的"主入口拼图"路线自洽)。**三类贴纸全部零依赖**:emoji 用系统字体 `fillText`、文字 `fillText`+半透明描边、手绘是 **12 个 canvas 路径图标**(`DOODLE_IDS`,单位空间 [-1,1] 自绘、可染色)——不引图片 / 不引字体 / 不引 SVG 外链,守住"单文件 + 零外链"红线。交互:仅在贴纸标签 + 有图时主画布响应指针(选中 / 拖动 clamp 0–1 / 缩放 / 改色 / 删除 / 改文字),最多 16 个;导出 `exportDataURL()` 置 `_exportClean` 标志把贴纸烤进图、但压掉编辑用的虚线选框(同步重画、无闪烁)。这是 **3.0.0 beta 收口第二步(Task 2)**;剩 #151 更多滤镜(纯数据)、#152 再加一个 AI(放最后)即 v3.0.0 beta 全部开发收口。
- **联网 AI 文案 B1 走 provider-agnostic BYOK、默认关、只发文字、失败回退端上**(v3.0.0-beta.14):南心"ai 继续做把"放行 #146(此前"先拼贴、再做 AI"的拼贴已 beta.13 落地)。**没把"选哪家 AI"做成一道要南心回答的题**——做成服务商无关:她在设置里粘 **API 地址(base)+ 模型 + 自己的 Key**,任何 OpenAI 兼容 `chat/completions` 服务可用。理由:① 不锁死一家、不替非程序员做选型;② "用你自己的钥匙"本就需她提供端点。**隐私 / 安全底线**:默认关(`settings.aiOnline=false`),开关开了才显示「🌐 联网 AI 灵感」按钮与配置;**当次授权**(每次开 App 头一回 `confirm` 把"将要发出去的那串文字"原文摊给她看);**只发文字、绝不发照片**(`aiContextLine()` 把 `getContext()` 只读快照——本就无像素 / 无 GPS 原始坐标——转成中文描述串);Key 由**南心自己粘**(代码 Claude 绝不代填任何 key/secret),`type="password"`、不在任何会提交的 form 内、只存本机 localStorage。**老实告知 CORS**:浏览器直连第三方 API 受 CORS 限制,部分服务商不允许网页直连 → 连不上时**自动回退**端上 `suggestCaptions()` + 诚实 toast,绝不卡死 / 静默失败;**AI 绝不作为核心功能前置**(端上「✨ 来点灵感」原样保留)。复用 v2.9.1 预留的 `window.Yinji.ai` 扩展点(注册 `id:"captionOnline"`、`onDevice:false`),零核心结构改动。**并按南心"『长按图片可看原图对比』不要去掉"保留** `.stage-hint`——了结此前一直挂着的"那行小灰字删不删",**结论:保留**。
- **多行水印文字走"改共享渲染入口、不逐款重写"**(v3.4.0):南心两次"直接做多行文字",兑现 v3.3.0 留的承诺(路线图 §3.7)。**核心难点**:48 款水印各自画字,逐款改两行体量 / 风险都大。**关键决策:先摸架构,发现带底栏的款(经典 / 徕卡 / 电影 / 胶片等约 19 款)全部走同一个共享函数 `drawManualText`——改它一处即覆盖大多数**,再单独适配 3 类自绘文字的款,而非 48 款各改一遍。① **输入层**:`#textInput` 由 `<input>` 改 `<textarea>`(回车天然换行、不在 form 内故不提交;`maxlength` 40→60;`autoGrowText` 随行数 1→3 自动增高、`scrollHeight>0` 才设防隐藏态塌);CSS 把 `input[type=text]` 选择器**扩到 `textarea`**(复用既有玻璃 / 深色 / 焦点样式,零新视觉)。② **渲染层**:抽 `mlLines`(切行 / 去空白行 / 截断 3)+ `mlFlat`(折一行);`drawManualText` 多行**按最宽行缩字 + 底栏内垂直居中**(n=1 与旧版几乎一致、不回归单行观感);`drawMinimalCorner`(右下角裸字)**从下往上叠** + 明暗采样区扩到整块文字(每行同色,延续 v3.2.0 自动黑白);`drawMagazineCover`/`drawMagazineLight` **第 1 行作大标题、其余作副标**(契合其既有 title+caption 两段布局,代码同形用 replace_all 一次改两处)。③ **单行语义款优雅降级**:`drawFootprint`/`drawBigDate`/`drawMinimalFrame`(地点 / 日期类,本就该一行)改用 `mlFlat` 把多行折成一行——多行粘贴不会乱、也不依赖 canvas 对 `\n` 的未定义渲染。**为何不做"加行高就撑高底栏"**:撑高需每款重算画布尺寸(侵入式、风险大),选"在既有底栏内缩字 + 居中"零侵入、覆盖面最广,2–3 行在常规底栏高度(`w*0.12~0.14`)下清晰够用。「**自选字段**」部分(§3.7 另一半)**早已具备**(设置页"自动模式显示哪些信息"六项开关即字段勾选),无需另做。纯本地 + 零外链;浏览器逐项 + 合成图像素带验证(经典 3 行居中 / 右下角叠 2 行 / 杂志分层 / 输入框 65→87px 自增 / 底栏扫出 2 条文字带 / 零报错)。**路线图 §3.7 多行部分完成**。
- **v3.5.0 选"内容包扩充"而非 §3.6 文字微调**(v3.5.0):南心"继续做 3.5.0"(未指定功能、由我挑)。评估路线图剩余项后**主动避开 §3.6 文字微调**——理由有二:① 路线图本身把它标注为"谨慎 / 如果会让普通用户困惑,宁可不做"(直接挑战"简洁直观"底线,与南心非程序员 + 重简洁的画像冲突);② **架构上不合**:水印文字本就由 `drawManualText`/`drawAutoText` 按底栏高度自适应缩放(已自动填满),再加"字号旋钮"要么与自适应打架、要么只能缩不能放(放会溢出固定底栏),价值低且 UI 变复杂。故选**最稳、可见、合简洁**的内容扩充——延续 v2.7.1/v2.10.0/v3.0.0-beta.4/v3.1.1 的"调色板 + 共享渲染器 / TONE 数据扩表"零风险加法:水印 48→54(`SEASON`+3 浅 / `BRAND`+3 深,共用 `drawSeasonStyle`/`drawBrandStyle`,改 `TEMPLATES`/`DRAW_DISPATCH`/`CAT_IDS`)、滤镜 32→38(只往 `TONE_LIST`+`TONE_PARAMS` 加数据,参数全落既有取值区间)。**全程不写新绘制、不动引擎**,被既有渲染 / 调色 / 随机 / 分享导入管线自动消费,零回归。浏览器像素级验证:54 水印 chip + 底栏 bg 与设计稿逐位一致、39 滤镜 chip + 纯灰上冷暖明暗方向正确、零报错。**取舍说明**:开放式"继续做"我按"价值 / 风险 / 合简洁度 / 合架构"四维选了最稳的一档,并在汇报里把更进取的方向(文字微调 / 风格包 / 真机适配)摊给南心,她可随时改方向。
- **v3.6.0 选"风格包(本地版)"——上版加款式后这版做真功能 + 提前落地 4.0 头牌**(v3.6.0):南心又"继续做下一版"(仍未指定)。考虑到 v3.5.0 已是内容包(加款式),这版**刻意做一个真功能而非再堆款式**,从上轮列给南心的候选里选 **#1 风格包**(我的风格)。**为何是它**:① 是真能力(把"调好的一整套观感"复用),不是又加几个款;② 复用现成基建(自定义滤镜 localStorage 范式 + `setStyle`/`recallStyleColor`/`applyAccent`),**不写新渲染、不动引擎、不碰红线**,落地稳;③ 它是 **4.0「成组与流程」的头牌之一**,提前做本地版既给南心即时价值、又为 4.0 验证概念、降风险。**关键设计决策**:① 风格包 = `{style, tone, toneAdj, filterId, accent}` 五元组(正好覆盖"看起来的样子");② `applyStylePack` 里 `setStyle` **不带 recall**——否则会触发 v3.2.0"按样式记忆"用该样式上次的配色覆盖本风格包要套的配色(两个记忆机制会打架),故风格包套用时显式绕开按样式记忆、以包内配色为准;③ **本地优先、分享后置**——roadmap-4.0 的风格包含"离线分享码",但分享要处理"包引用了自定义滤镜 / 水印而对方没有"的内嵌兜底,复杂度集中在这;故 v3.6.0 先做本地存 / 套 / 删(核心 80% 价值、robust),分享码留 4.x(同我做多行文字时"先做扎实的一半"的思路);④ **UI 守简洁**:放「水印」页顶部一个 `.tone-block`(复用现成类、零新 CSS),空态**不放任何灰字说明**(南心一贯偏好),只一个"存为当前风格"按钮。与既有"🎲 随机一套"互补(随机=碰新搭配 / 风格包=固化自己那套)、与"按样式记忆"分层(后者是每样式自动记色、前者是跨样式的命名整套)。纯本地 + 零外链;浏览器端到端验证(存→改→一键套用恢复 水印 bg + 滤镜暖度 + 主题色、删除、零报错)通过。**4.0 风格包本地版完成**,分享码 + 批量联动留 4.x。
- **v3.7.0 补齐风格包"离线分享 / 导入"——上版刻意留的另一半**(v3.7.0):南心部署完 v3.0.0~v3.6.0 后"接着做下一版"。选 v3.6.0 风格包**当时刻意推迟的离线分享码**作为最自然的续作(把一个功能做完整,而非开新坑)。**关键设计取舍**:① **滤镜免内嵌**——风格包存的 `tone`+`toneAdj` 是实际调色数值,能复现**任意**滤镜(内置 / 自定义)的观感(因 `applyCustomFilter` 本质就是设 tone+toneAdj),故分享码**不含 filterId**(导入端置 null、只影响 chip 高亮不影响成像),无需把自定义滤镜定义塞进码里——省码长、免一类内嵌兜底;② **只内嵌自定义水印**——54 款内置水印按 id 走(对方都有);唯独自定义水印(`custom_*`)对方没有,故 `encodePack` 检测到 style 是自定义时**内嵌 `styleTpl`**,导入端自动新建 `custom_*` 模板并把风格指向它(复用 importTemplateObj 的构造逻辑);③ **兜底**——导入的风格若 style 既非内置也非自定义(且无内嵌)→ 回落 `classic`,不让 draw 崩;④ **复用而非新造**——分享走既有 `SHARE_PREFIX="YINJI1:"` + base64 + `doImport` 按 `type` 路由(水印 / 滤镜已两条,加第三条 `yinji-pack`),一个输入框认三种码;UI 加 `#importPackBtn`(常驻)+`#sharePackBtn`(选中显),与"我的滤镜"的分享 / 导入按钮范式一致。纯本地、零外链(就是一段文字码,复制粘贴)。浏览器端到端验证:分享码生成 / 解码正确(231 字符)、**模拟另一台设备**(清 stylePacks 重载)导入后风格一字不差 + 套用逐位恢复、**自定义水印内嵌**导入后 customTemplates+1 + 套用底栏色逐位一致、零报错。**至此「我的风格」完整**(存 / 套 / 删 / 分享 / 导入);仅剩"与批量联动"待 4.x 批量功能落地时做。
- **批量加水印走"复用单张管线逐张跑 + 零依赖 store-zip",入口复用当前效果而非另造批量视图**(v3.8.0):南心部署完 v3.x 后直接点名"批量加水印"——4.0「成组与流程」头牌、roadmap §3.9 标注的"重活"。**关键架构决策**:① **不另造渲染**——批量 = 逐张走现成单张管线(`setStyle`/`getSize`/`gradedBitmap`/`draw`/贴纸全继承),只是循环 + 换 `state.bitmap`/`state.exif`,这样 54 水印 / 38 滤镜 / 贴纸 / 主题全自动支持、零回归;② **入口复用"当前编辑器这套效果"而非 roadmap-4.0 设想的"首页多选分流 + 批量编辑视图 + 缩略图网格"**——后者 UI 重(对非程序员 + 简洁底线不友好),而"先在编辑器调好一张、再点批量套到多张"心智更简单、几乎零新 UI(一个按钮 + 一个进度浮层),故选轻方案(网格预览等留 4.x 按需);③ **手机内存硬约束 → 逐张处理即时释放**(`renderOneForBatch` 渲一张取 JPEG 字节后立刻 `bmp.close()`,`setTimeout` 串行让出主线程 + 利于 GC,`BATCH_MAX=30` 上限),不同时持有 N 个大 canvas;④ **打包用自写零依赖 store-zip**(`crc32` 查表 + `makeStoreZip` 手写本地头/中央目录/EOCD、method 0 不压缩)——**绝不引压缩库**(守"单文件 + 零依赖"红线);⑤ **导出走 zip 单文件下载**而非"逐张存相册"——iOS 多次下载/存相册不可靠,zip 一次到位(代价:iPhone 需到「文件」长按解压,已在文案老实告知,且留"逐张存相册"作 4.x 备选待南心反馈);⑥ **文字策略尊重当前模式**(auto 各读各 EXIF / manual 同文案),不强制。纯端上、**照片绝不上传**、零外链。浏览器端到端验证:批 3 张 → 拦截的 zip blob 解析合法(EOCD 签名 + 3 本地头 + 3 条目 + 首条 `yinji_001.jpg` 数据 FFD8)、解出首条 JPEG 采样底栏色 = 所选样式(水印真套上)、批量完编辑器还原原图、零报错。**4.0 支柱 1 本地版完成**;缩略图网格 / 逐张存相册 / 批量套风格包 / 保原图 EXIF 留 4.x。
- **液态玻璃做成"强度可调"而非重写,iOS27 适配走 CSS 近似**(v3.9.0):南心"适配 iOS27 液态玻璃效果可调"。**关键判断**:① 现有玻璃**早已变量驱动**(`--glass-blur`/`--glass-blur-sm` + saturate + brightness + 高光鳞边),且所有主玻璃面(面板/弹层/芯片/输入框)都读这俩变量——所以"可调"的最省做法是**用 CSS 变量按档覆盖强度**(新增 `body[data-glass-level="light"|"strong"]` 覆盖 blur/饱和/鳞边),改一处、整套 UI 一起变,**不逐规则改、不动渲染管线**;② **标准档刻意 = `:root` 默认值、不另写 block**,保证老用户(默认标准)观感零变化、不被"升级"惊到;③ **只覆盖 模糊/饱和/鳞边(主题无关量),不动 `--glass-bg` 透明度**——因深色主题已覆盖 bg alpha,动它会跨主题打架,而模糊+饱和+鳞边正是"强度"最直观的信号。④ **iOS27 适配 = 强档**(更大模糊 64px + 更高饱和 210% + 更亮高光鳞边),因为**真·Liquid Glass 的折射/流动需 SVG 置换或 WebGL,会破"单文件 + 零依赖"红线**,网页里 CSS 近似(强模糊 + 高饱和 + 亮鳞边)是最接近且最稳的——这是明确的红线取舍,不为追求像素级还原而引重依赖。⑤ **可调 UI** 把设置里玻璃从 开/关 复选框换成 关/轻/标准/强 分段(与"主题"控件同款),`GLASS_KEY` 存 level 字符串并**兼容老存档**("0"→off、"1"→standard),老用户平滑迁移。⑥ **删 `var glassToggle` 后**,`syncSettingsUI` 里那行 `glassToggle.checked=…` 必须改成 `syncGlassSeg(…)`,否则严格模式下引用未声明变量会 ReferenceError(已改、已验证设置面板不报错)。只动 App 界面外观、**不影响照片成品**。浏览器逐项验证(四档 blur none/16/44/64 在真实面板生效、默认标准、老存档迁移、零报错)。南心需求完成。
