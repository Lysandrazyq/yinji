# 印迹 · 照片水印 —— 项目说明（CLAUDE.md）

> 本文件供 AI（Claude）在每次参与本项目开发时**最先阅读**。
> 它说明项目是什么、各标准文件在哪、以及必须遵守的工作规矩。

> ⚠️ **协作须知**：本项目有两个 Claude 协作——你负责**写代码**，另一个负责**项目管理**（早报/晚报/看板）。你们的对接方式见 `docs/双Claude协作说明.md`，**开工前务必先读**。

---

## 一、项目概况

「印迹」是一个运行在浏览器里的**照片水印网页 App**，主要面向 iPhone Safari，可"添加到主屏幕"当原生 App 用。

核心能力：给照片加一条**白框黑字**的底栏水印（类似 OPPO / 徕卡相机水印）。支持两种文字来源——手动输入、或自动读取照片 EXIF（设备、时间、光圈、快门、ISO、焦段），一键切换、实时预览。所有处理都在用户手机**本地**完成，照片不上传。

项目负责人是**非程序员**。沟通和交付都要照顾这一点（见第三节）。

---

## 二、项目结构与文件路径

```
yinji/
├── CLAUDE.md                 ← 本文件(工作总指引)
├── watermark.html            ← 应用本体(单文件,HTML+CSS+JS 全部内联)
├── manifest.webmanifest      ← PWA 元数据(App 名 / 图标 / 主题色)
├── sw.js                     ← PWA service worker(离线缓存)
├── package.json              ← Node 依赖清单(Tauri,可选)
├── icons/                    ← PWA 图标
│   ├── icon.svg              ← 普通图标
│   └── icon-mask.svg         ← maskable(安卓自适应)
├── docs/                     ← 标准文档(开工前按需阅读),每份 .md + .docx 双格式
│   ├── requirements.md / .docx       ← 需求、范围、路线图
│   ├── tech.md         / .docx       ← 技术选型、实现要点、平台接口、PWA
│   ├── design.md       / .docx       ← 视觉规范、配色、模板样式
│   ├── steps.md        / .docx       ← 开发 / 自检 / 部署 / 协作流程
│   ├── cross-platform.md / .docx     ← Win / PWA 打包指南(详细)
│   └── tauri.conf.example.json       ← Windows Tauri 配置范例
├── tools/
│   └── md2docx.py            ← 把 docs/*.md 重新生成同名 .docx(改完 md 后跑一下)
└── devlog/                   ← 更新日志(中文名双版)
    ├── 更新日志-用户版.md      ← 每天一条 App Store 风格总结(带日期)
    └── 更新日志-开发版.md      ← 按版本倒序的详细技术变更(带日期)
```

> `.docx` 是给负责人用 Word 打开看的,**由 .md 自动生成**,不要手动编辑。改完任何一份 .md,交付前都要跑 `py tools/md2docx.py` 重新生成(详见 `docs/steps.md` 3.5 节)。**⚠️ 用 `py` 不要用 `python`**——本机 `python` 现指向缺 `python-docx` 的 venv(`hermes-agent\venv`),会报 `No module named 'docx'`;`py`(系统 Python313)才装了 `python-docx`。另:Bash 默认 cwd 是 `E:\yinji\yinji-project`(父目录),跑项目内命令需 `cd /e/yinji/yinji-project/yinji`。

> **跨平台**:核心永远是 `watermark.html`。PWA / Tauri 两种"装上"方式都只是套壳,不改核心(安卓 Capacitor 路线已弃用)。平台差异通过 `window.Yinji.platform.*` 接口注入,详见 `docs/cross-platform.md`。

> `.docx` 是给负责人用 Word 打开看的，**由 .md 自动生成**，不要手动编辑。改完任何一份 .md，交付前都要跑 `python tools/md2docx.py` 重新生成（详见 `docs/steps.md` 3.5 节）。

各文档用途速查：
- 改需求 / 加功能前 → 读 `docs/requirements.md`
- 动代码前 → 读 `docs/tech.md`
- 调样式 / 配色 / 排版前 → 读 `docs/design.md`
- 不确定怎么测、怎么发布、怎么和负责人协作 → 读 `docs/steps.md`

---

## 三、给 AI 的工作规矩（每次都要遵守）

1. **先读后做**：开工先读本文件，再读 `docs/` 下相关标准文件，确认改动符合既有需求与规范，不凭空发挥。

2. **面向非程序员沟通**：用大白话，不堆术语；涉及取舍先讲清楚利弊；**改动前先确认需求，得到同意再执行**。交付时给清楚的"怎么用"步骤。

3. **单文件 + 零外部依赖**（重要，踩过坑）：App 必须是一个自包含的 HTML 文件，**不引入任何需要联网才能加载的外部资源**（如外链字体、CDN 脚本）。字体一律用系统字体栈。原因：外链资源会在手机内置浏览器里阻塞渲染，导致白屏。

4. **隐私优先**：所有照片处理留在本地，绝不上传照片到任何服务器。

5. **改完必自检**（详见 `docs/steps.md`）：至少做到 JS 语法检查、EXIF 解析用样张验证、水印排版肉眼检查。

6. **写更新日志**（这就是"每天自动记录"的落地方式）：**每次完成一轮开发后**，更新 `devlog/` 下的两份日志——`更新日志-用户版.md`（给负责人看，当天加 / 补一条 App Store 风格的大白话总结，带日期）和 `更新日志-开发版.md`（给维护者看，在最上方加一节「日期 · 版本号」的详细技术变更）。这一步每次都要做，不要遗漏。

---

## 四、当前状态（摘要，详情见最新开发日志）

- **正式版网址**:https://lysandrazyq.github.io/yinji/watermark.html(GitHub Pages 部署,所有平台浏览器打开即用)
- **当前版本**:**v3.20.0**(**液态玻璃交互动效**——按压波纹+弹性回弹+光线追踪+长按形变。`APP_VERSION=v3.20.0`、`sw.js`→`yinji-v61`。纯CSS材质反馈,无JS涟漪/追踪。详见 `CHANGELOG.md`)——工具栏收进🔧下拉菜单;撤销下线修bug;标签栏按钮精简。`APP_VERSION=v3.19.0`、`sw.js`→`yinji-v60`。详见 `CHANGELOG.md`)——🤖输入描述AI自动搭配水印+滤镜+调色;📤批量处理后一键分享到微信。`APP_VERSION=v3.18.0`、`sw.js`→`yinji-v59`。详见 `CHANGELOG.md`)——撤销+旋转/翻转+裁剪+水印位置+批量预览+自定义水印取色。`APP_VERSION=v3.17.0`、`sw.js`→`yinji-v58`。详见 `CHANGELOG.md`)——批量处理完后可选「📱逐张保存」直存相册,不用zip解压;iPhone更友好。`APP_VERSION=v3.16.0`、`sw.js`→`yinji-v57`。详见 `CHANGELOG.md`)——水印110→95、滤镜94→87,删掉趋同/重复项;编辑器标签栏加「📋 浏览全部」按钮,右侧滑出抽屉面板,水印/滤镜一屏看全+搜索。`APP_VERSION=v3.15.0`、`sw.js`→`yinji-v56`。详见 `CHANGELOG.md`)——浅色水印+25(奶白/樱粉/柠檬冰/薄荷霜/薰衣雾/桃花/鸽灰/香草/海雾/蔷薇尘/橄榄枝/婴儿蓝/沙丘/紫藤/绿茶/珊瑚晕/冰雾蓝/蜂蜜金/鼠尾雾/兰花紫/苏打奶/晨霞/海玻璃/粉蓝/麦田);深色水印+25(墨耀/石墨/午夜蓝/勃艮第/森绿/钢青/可可/紫晶/炭灰/红木/海军蓝/乌梅/青铜/板岩/暗苔/石榴红/墨池/松露/暮光/松果/深渊蓝/余烬/接骨木/漂木/鸦羽);滤镜+50(胶片8+黑白6+暖色8+冷色8+清新8+风格12)。纯数据扩表、零新绘制、零改引擎。`APP_VERSION=v3.14.0`、`sw.js`→`yinji-v55`。详见 `CHANGELOG.md`)
- **本轮新增**(v3.19.0):**UI精简**——工具栏4按钮(旋转/翻转/裁剪)收进`#toolsDrop`>`#toolsMenu`下拉(点🔧展开/点别处或按钮自动收);`#undoBtn`注释下线;`#filterRandBtn`→`🎲`/`#drawerBtn`→`📋`只留图标;撤销相关pushUndo钩子/setStyle.setTone.applyCustomFilter覆写/tonePad pointerdown/up全注释。删`.toolbar-row`/`.tb-btn`CSS→加`.tools-drop`/`.tools-menu`。SYNTAX_OK。
- **上一版**(v3.18.0):**AI一句话生成样式+批量分享**——①`aiStyleGen()`:prompt输入描述→`aiOnlineReady`校验→`aiContextLine`照片上下文→构建styleList+filterList给AI→fetch→解析JSON{style,tone,temp,tint,sat,intensity,reason}→校验id+套用;②`batchShareAll()`:`_batchEntries`存结果→`navigator.share({files})`系统分享面板(≤10张);`#aiStyleBtn`+`#batchShareAll`按钮。零加载期外链,SYNTAX_OK。
- **上一版**(v3.17.0):**6合1工具包**——①撤销:undoStack/L30+pushUndo钩setStyle/setTone/applyCustomFilter+tonePad pointerdown/up+undoBtn;②旋转/翻转:rotateBitmap(90/270)/flipBitmap+rotateLeftBtn/RightBtn/flipBtn;③裁剪:cropOverlay/cropCanvas+cropRatio(null/free/1:1/4:5/16:9/3:2)+openCrop/renderCropPreview/applyCrop;④水印位置:state.wmPosition("bottom"/"top"/"center")+wmPosSeg+drawSeasonStyle/drawBrandStyle改photoY/barY;⑤批量预览:showBatchPreview(files)读FileReader缩略图插入sheet;⑥自定义取色:openCustomEditor劫持注入.bg/text color input。零加载期外链,SYNTAX_OK。
- **上一版**(v3.16.0):**批量逐张保存**——`renderOneForBatch`改返`{bytes,dataUrl}`;`finish`显示📱逐张保存+📦zip双按钮;`saveIndividually`(`_batchSaveIdx`+`_batchSaveList`+`_saveNextBatch`600ms间隔防拦截+进度);`dataURLToBlob`;`closeBatch`重置按钮。`#batchActions`含`#batchSaveEach`+`#batchSaveZip`。
- **上一版**(v3.15.0):**去重+抽屉**——去重:删15款趋同浅色水印(iceMist/doveGray/dawnBlush/seaMist/peachBloom/lemonIce/wheatField/wisteria/greenTea)+6款趋同深色(obsidian/twilight/cocoa/graphite/admiral/driftwood)+7款趋同滤镜(polaroidFilm/bleachBypass/bwBlue/paprika/fireGlow/arctic/winterFrost),水印110→95/滤镜94→87。抽屉:`#drawerOverlay`>`#drawerPanel`(右滑出,92vw×380px全高,复用既有玻璃变量+独立CSS)→tab切水印/滤镜→`drawerGrid`(flex-wrap按钮网格,水印带swatch色块、滤镜带分类标签)→`drawerSearch`输入实时过滤→点item套用+关抽屉。`openDrawer`/`closeDrawer`/`renderDrawerGrid`。零加载期外链、SYNTAX_OK。
- **上一版**(v3.14.0):**内容大包 水印 60→110(+50) + 滤镜 44→94(+50)**——SEASON +25 palettes(creamWhite~wheatField,全走 `drawSeasonStyle`)、BRAND +25 palettes(obsidian~raven,全走 `drawBrandStyle`);TEMPLATES +50 条、DRAW_DISPATCH +50 条(season×25→drawSeasonStyle/Brand×25→drawBrandStyle)、CAT_IDS season+25/dark+25;TONE_LIST +50 条(胶片8/黑白6/暖色8/冷色8/清新8/风格12)、TONE_PARAMS +50 组(参数全落既有区间 sat 0–1.30 con .89–1.18 bri .94–1.08 temp −26–30 tint −4–10 lift 0–28)。自检 SYNTAX_OK、LINT_OK。纯数据扩表、零新绘制、零改引擎。
- **上一版**(v3.13.0):**风格包×批量联动 · 4.0 收尾**——编辑器批量按钮上方加 `#batchPackBlock`(`.tone-block`,零新CSS):「当前效果」chip + 已存风格包 chips;`batchPackId`(null=当前)+`renderBatchPackChips`(空包自动隐藏);`runBatch` 开头若选中包→备份→`applyStylePack`→`finish` 还原。自检 SYNTAX_OK、LINT_OK。4.0 全部收口。
- **更早**(v3.10.0):——南心点名"文字微调"(路线图 §3.6)。`state.wmTweak={barScale:100,fontScale:100}` 新键 `yinji.wmTweak.v1`;`tweakBarH(base)`/`tweakFont(base)` 两个 helper 在所有 13 处 barH 计算 + 5 处 font px 计算各乘一次比例;「水印」页底部加折叠「高级微调 ▸」,展开两 range slider(50–200%)+「恢复默认」。**只加全局缩放层、不改任何逐款渲染逻辑,54 款 + 自定义模板全部自动继承,零新 CSS**。自检:SYNTAX_OK、LINT_OK、浏览器拉 slider 实时出图、console 零报错。`APP_VERSION=v3.10.0`、`sw.js`→`yinji-v51-2026-06-09-v3.10.0`。3.x 最后一个待做项(除 §3.3 真机适配和阻塞的图四/图五)完成。详见 `CHANGELOG.md`)
- **更早**(v3.10.0 细节):**文字微调**——data:`state.wmTweak{barScale:100,fontScale:100}`+`WMTWEAK_KEY`+`loadWmTweak`/`saveWmTweak`。helper:`tweakBarH(base)`=`Math.round(base*barScale/100)`;`tweakFont(base)`=`Math.round(base*fontScale/100)`。barH:13 处全包(shared renderers×2 + 各自定义 renderer + drawCustom tpl.barRatio)。font:`drawManualText` targetPx 用 tweakFont(min 9px 闸)+`drawAutoText` 4 处(dpx/tpx/rpx/fallback)全部 tweakFont。UI:`#tweakBlock`(.tone-block 复用)→toggle「高级微调 ▸/▾」→range sliders(50–200%,step5,accent 色)→bar/font 当前值 label→「恢复默认」。接线:`initWmTweak` 自包含,slider input→写 state→saveWmTweak→draw(实时);reset→写回 100/100。启动:loadWmTweak() 挂 loadStylePacks() 后。**零新 CSS 规则、零逐款回归、零碰引擎**。自检 SYNTAX_OK、tweakBarH 14/tweakFont 10/WMTWEAK_KEY 3/wmTweak 11 refs、LINT_OK;浏览器 slider 拉 barScale 150%/fontScale 150%→实时出图、恢复默认→回 100%、barScale 50% 明显矮栏、console 零报错。路线图 §3.6 完成。
- **更早**(v3.9.0):**液态玻璃强度可调**——CSS:`body[data-glass-level="light"]`(blur16/10、saturate130/125、淡鳞边)+`["strong"]`(blur64/42、saturate210/195、亮鳞边 `inset 0 1.5px 0 rgba(255,255,255,.72)`+border .46);标准=`:root`(blur44/30、saturate180)。`data-glass="off"` 仍走纯色路径。JS(原 glassToggle 块重写):`glassSeg`/`GLASS_KEY`/`GLASS_LEVELS`/`normGlassLevel`(0→off、1/null→standard、非法→standard)/`glassLabel`/`applyGlass(level)`(off:data-glass=off+移除 level;否则 data-glass=on+data-glass-level=level;写 localStorage)/`syncGlassSeg`(切 `.on`)/initGlass(读存档→applyGlass→syncGlassSeg)/click 监听(自包含,不用 segBind);`syncSettingsUI` 改用 `syncGlassSeg`。HTML:`.set-lbl 液态玻璃`+`.seg#glassSeg`(data-g off/light/standard/strong,同主题分段控件)。自检 SYNTAX_OK、glassToggle 残留=0、符号齐、外链=NONE。浏览器逐项(默认标准/四档 blur none-16-44-64/真实面板生效/设置无报错/老存档迁移/零报错)。路线:南心"适配 iOS27 液态玻璃可调"完成。
- **更早**(v3.8.0):(**批量加水印(4.0「成组与流程」头牌)**——南心部署完 v3.0~v3.7 后点名"批量加水印"。**架构:不另造渲染,复用单张管线逐张跑 + 自写零依赖 store-zip 打包**。入口复用"当前编辑器这套效果"(不另造样式选择器):保存区加「🗂️ 把这套效果批量加到多张照片」+ 隐藏 `#batchInput multiple`;选多张 → 每张套**同一套** style/tone/toneAdj/filterId/stickers + 主题色、**不改 mode**(auto 各读各 EXIF / manual 同文案)。`renderOneForBatch`:`parseExif`+`loadBitmap`→`state.bitmap/exif`→`invalidateGrade`→`draw`→`canvas.toDataURL(jpeg,.92)`→`dataURLToUint8`→`bmp.close()`;`runBatch` `setTimeout(next,0)` 串行(让出主线程 + 省内存)、单张失败跳过、备份/还原 state、`_exportClean` 防选中框、`BATCH_MAX=30`。`makeStoreZip`=查表 crc32 + 手写 ZIP(本地头/中央目录/EOCD,method 0 不压缩,ASCII 名),输出 Blob 走 `downloadBlob`,**零压缩库、守零依赖**。`#batchOverlay` 进度条。纯端上、**照片绝不上传**。浏览器验:批 3 张 → 拦截 zip 解析(EOCD OK / 本地头 3 / 条目 3 / 首条 `yinji_001.jpg` 且 FFD8)、解出首条 JPEG 采样底栏 = skyBlue 逐位(水印真套上)、批后编辑器还原、零报错。`APP_VERSION=v3.8.0`、`sw.js`→`yinji-v49-2026-06-09-v3.8.0`。详见 `CHANGELOG.md`)
- **更早**(v3.8.0 细节):**批量加水印 · 4.0 支柱 1 本地版**——HTML:保存区 `.actions` 下加 `#batchBtn`「🗂️ 把这套效果批量加到多张照片」+ `#batchInput`(hidden,multiple)+ `#batchOverlay`(`.overlay/.sheet` + `.batch-bar` 进度)。JS(置 `downloadBlob` 后):`crc32`(查表)、`makeStoreZip(files)`(本地文件头 0x04034b50 + 中央目录 0x02014b50 + EOCD 0x06054b50,store/method 0,UTF-8 安全但用 ASCII `yinji_NNN.jpg`)、`renderOneForBatch(file)`(复用单张管线渲一张取 JPEG 字节 + `bmp.close()` 释放)、`runBatch(files)`(过滤图片类型、`BATCH_MAX=30` 截断、备份 state、`_exportClean=true`、`setTimeout(next,0)` 串行渲染 + 进度、`finish` 还原 state+draw + `makeStoreZip`→`downloadBlob(印迹_批量_日期.zip)`)、`openBatch/setBatchProgress/setBatchDone/closeBatch`。`batchBtn` 点击校验有图 → `batchInput.click()`;`batchInput` change → `runBatch`。**逐张处理即时释放(手机内存约束)、单张失败跳过、不改渲染管线、不引压缩库**。自检 SYNTAX_OK、符号齐、外链=NONE。浏览器:zip 合法 3 条 + 首条 JPEG、解出底栏 skyBlue 逐位、编辑器还原、零报错。路线:**4.0 支柱 1 本地版完成**;4.x 可选:结果逐张存相册(替 zip,iPhone 友好)、批量套风格包、保原图 EXIF。
- **更早**:**v3.7.0**(**风格包离线分享 / 导入**——南心部署完 v3.0.0~v3.6.0 后"接着做下一版"。补齐 v3.6.0 风格包**刻意留的另一半 = 离线分享码**,复用 `SHARE_PREFIX="YINJI1:"` + base64 + `doImport` 按 type 路由(加第三条 `yinji-pack`)。**关键设计:滤镜免内嵌**(`tone`+`toneAdj` 已复现任意滤镜观感,分享码不含 filterId)、**只内嵌自定义水印**(内置 54 款按 id 走;`custom_*` 才内嵌 `styleTpl`,对方导入端自动新建 `custom_*` 模板并指向)。`encodePack`/`sharePack`/`importPackObj`(兜底:对方没有的水印回落 classic);`#packActions` 加 `#importPackBtn`(常驻)+`#sharePackBtn`(选中显)。纯本地零外链。浏览器验:造 skyBlue+琥珀风格→分享码 231 字符解出 yinji-pack、**模拟另一台设备导入** → 风格一字不差 + 套用底栏 bg 逐位恢复 + 暖度恢复、**自定义水印内嵌**(造 styleTpl#FF2299 码导入→ customTemplates+1 + 套用 bg 逐位一致)、零报错。`APP_VERSION=v3.7.0`、`sw.js`→`yinji-v48-2026-06-08-v3.7.0`。详见 `CHANGELOG.md`)
- **更早**(v3.7.0 细节):**风格包离线分享 / 导入 · 4.0 风格包补齐**——`encodePack(p)`=`{v:1,type:"yinji-pack",pack:{name,style,tone,toneAdj,accent,styleTpl?}}`→UTF-8 base64→`YINJI1:`;`findCustomTemplate(p.style)` 真才内嵌 `styleTpl`(滤镜免内嵌:tone+toneAdj 已复现观感)。`sharePack(id)`→`openShare("export",code,"pack")`(openShare noun 加 pack→「风格」)。`doImport` 路由加 `yinji-pack`(置 filter/template 前)→`importPackObj`:带 styleTpl 且本地无→校验后新建 `custom_*`+persist+renderStyleSeg+指向;`style` 非内置(`presetName`)非自定义(`findCustomTemplate`)→兜底 classic;建 pack(clamp/校验 accent)+persist+render+closeShare+toast。UI:`#importPackBtn`(常驻「导入风格」,`openShare("import","","pack")`)+`#sharePackBtn`(选中显,`sharePack(packSel)`);`updatePackActions` 同切 del+share。自检 SYNTAX_OK、符号齐、外链=NONE。浏览器:分享码 231 字符/解出 yinji-pack/{skyBlue,amber,accent,无 styleTpl};另设备导入 → 一字不差 + 套用逐位恢复(bar 227/237/246 + 暖度);自定义水印内嵌导入 → customTemplates+1 + bar #FF2299 逐位;零报错。路线:**4.0 风格包(本地 v3.6.0 + 分享 v3.7.0)完整**,仅剩"与批量联动"待 4.x。
- **更早**:**v3.6.0**(**「我的风格」风格包(本地)**——南心"继续做下一版"(未指定、由我挑)。上版加款式,这版选候选 #1 **风格包**:**真功能、非堆款**,且是 **4.0「成组与流程」头牌之一提前落地**。把当前"看起来的样子"= `{style, tone, toneAdj, filterId, accent}` 一套存成命名风格,一键套用(同时恢复 水印 + 滤镜 + 调色 + 主题色)。**复用自定义滤镜 localStorage 范式 + 既有 setStyle/recallStyleColor/applyAccent,不写新渲染、不动引擎、纯本地**(本地版先行,离线分享码留后续)。新键 `yinji.stylePacks.v1`;`#watermarkPane` 顶加 `#packBlock`(复用 `.tone-block`、零新 CSS、空态无灰字)。`applyStylePack` setStyle **不带 recall**(免被 v3.2.0 按样式记忆覆盖) + 镜像 recallStyleColor 恢复滤镜调色 + `applyAccent`。自检 SYNTAX_OK、外链=NONE。浏览器验:存(localStorage 存全套)、改成 classic+原图后一键套用 → 底栏 bg 逐位恢复 skyBlue(227/237/246)+ 暖度恢复 amber(52)+ `--accent` `#C2632D`→`#2E6F95`、删除 2→1、零报错。`APP_VERSION=v3.6.0`、`sw.js`→`yinji-v47-2026-06-08-v3.6.0`。详见 `CHANGELOG.md`)
- **更早**(v3.6.0 细节):**「我的风格」风格包 · 4.0 头牌提前落地(本地版)**——新键 `yinji.stylePacks.v1`(独立、防迁移回归);`stylePacks`/`packSel`、`isValidPack`/`loadStylePacks`/`persistStylePacks`(镜像 customFilters)。`captureStylePack()`=`{style:state.style, tone:state.tone, toneAdj{temp,tint,sat,intensity}(clamp), filterId:state.filterId, accent:settings.accent}`。`applyStylePack(p)`:① `setStyle(p.style)` **不带 recall**(关键:免被"按样式记忆"覆盖本套配色);② 镜像 `recallStyleColor` 恢复 tone/toneAdj/filterId(校验 `TONE_PARAMS`/`findCustomFilter`)+ `syncTonePad`/`markFilterActive`/`updateFilterActions`;③ `settings.accent=p.accent;saveSettings();applyAccent();syncSettingsUI()`;末 `draw()`+`savePrefs()`。UI:`#packBlock`(`.tone-block`)= 头"我的风格" + `#packSeg` chips + `#savePackBtn`(常驻)+ `#delPackBtn`(选中才显),**空态无灰字**(南心偏好);`renderPackChips`/`saveCurrentAsPack`(prompt≤20,`pack_<ts36>` id)/`deleteSelectedPack`(confirm);启动 load+render+bind。自检 SYNTAX_OK、符号齐、外链=NONE。浏览器(合成纯灰照):空态正确、存为风格(1 chip+localStorage 全套)、一键套用恢复 水印 bg+滤镜暖度+样式高亮、含 accent 的 pack 套用 `--accent` 变 #2E6F95、删除 2→1、零报错。路线:**4.0 风格包本地版完成**;离线分享码版留后续(需处理 pack 引用自定义滤镜 / 水印的内嵌兜底)。
- **更早**:**v3.5.0**(**内容包:水印 48→54 + 滤镜 32→38**——南心"继续做 3.5.0"(未指定、由我挑)。评估路线图后**避开 §3.6 文字微调**(标注"谨慎/宁可不做"、且水印文字本按底栏自适应、字号旋钮与设计相悖),选最稳可见合简洁的内容扩充,延续"调色板 + 共享渲染器 / TONE 数据扩表"零风险加法、**不写新绘制、不动引擎**。水印 +6:`SEASON` +3 浅(晴空 / 香芋 / 陶玫,共用 `drawSeasonStyle`)、`BRAND` +3 深(摩卡 / 苔墨 / 暗玫,共用 `drawBrandStyle`);改 `TEMPLATES`/`DRAW_DISPATCH`/`CAT_IDS`。滤镜 +6:只往 `TONE_LIST`+`TONE_PARAMS` 加数据(过期卷 / 琥珀 / 冰蓝 / 通透 / 暗夜 / 丝绒,参数全落既有区间)。自检 SYNTAX_OK、加载期外链=NONE。浏览器验:54 水印 chip + 6 新名、底栏角像素与设计 bg **逐位一致**;39 滤镜 chip + 6 新名、纯灰上冷暖明暗方向正确;零报错。`APP_VERSION=v3.5.0`、`sw.js`→`yinji-v46-2026-06-08-v3.5.0`。详见 `CHANGELOG.md`)
- **更早**(v3.5.0 细节):**内容扩充(纯数据扩表,零新绘制)**——水印:`SEASON` +`skyBlue`{#E3EDF6}/`lavender`{#ECE9F4}/`clayRose`{#F4E6E0}、`BRAND` +`espresso`{#1F1611,acc#C8956A}/`oliveDark`{#191B12,acc#9AA458}/`dustyRose`{#1E141A,acc#C77E92};`TEMPLATES`+6、`DRAW_DISPATCH`+6(→`drawSeasonStyle`/`drawBrandStyle`)、`CAT_IDS` season +3 浅 / dark +3 深(沿 wineRed 等"氛围深色归 dark"惯例)。滤镜:`TONE_LIST`+6 + `TONE_PARAMS`+6——expired(film,tint+11/lift20)、amber(warm,temp+24)、iceBlue(cool,temp-18/bri1.05)、clearDay(fresh,bri1.07)、noir(mood,sat0.5/con1.16/bri0.96)、velvet(mood,temp+8),全落既有取值区间(sat 0–1.35/con .86–1.24/bri .95–1.08/temp −28–32/tint −5–11/lift 0–30)。自检:6 水印 id 各 4 处、6 滤镜 id 各 2 处、SYNTAX_OK、外链=NONE。浏览器(合成纯灰照):54 水印 chip、底栏 bg 逐位一致(晴空 227/237/246 … 暗玫 30/20/26);39 滤镜 chip、纯灰方向(琥珀 warmth+52 / 冰蓝 −37 / 通透 +38 / 暗夜 −9 / 丝绒 +16 / 过期卷 绿偏);零报错。路线:§3.1 类内容扩充。
- **更早**:**v3.4.0**(**手动水印文字支持多行**——南心两次"直接做多行文字",兑现 v3.3.0 承诺(§3.7)。**关键决策:抓共享渲染入口 `drawManualText`**(经典 / 徕卡 / 电影 / 胶片等约 19 款底栏都走它)改一处即覆盖大多数,再单独适配 3 类自绘款。`#textInput` `<input>`→**`<textarea>`**(回车换行、最多 3 行、`autoGrowText` 随行数增高);新增 `mlLines`(切行 / 去空 / 截断 3)+`mlFlat`(折一行)。`drawManualText` 多行居中 + 按最宽行缩字(n=1 不回归);`drawMinimalCorner` 从下往上叠 + 整块明暗采样自动黑白;杂志封面 / 杂志·浅 第 1 行作标题、其余作副标;足迹 / 大日期 / 极简框 单行语义款用 `mlFlat` 优雅降级。纯本地 + 零外链。浏览器验:textarea 就位、1 行 65px→3 行 87px 自增、spy `fillText` 经典 3 行居中 / 右下角叠 2 行 / 杂志分层、成品像素扫出 2 条文字带、零报错。`APP_VERSION=v3.4.0`、`sw.js` CACHE→`yinji-v45-2026-06-08-v3.4.0`。详见 `CHANGELOG.md`)
- **更早**(v3.4.0 细节):**多行水印文字 · 路线图 §3.7**——**输入层**:`#textInput` `<input type=text maxlength=40>`→`<textarea rows="1" maxlength="60">`(placeholder 注明回车换行最多 3 行;不在 form 内,回车不提交);CSS 把 `input[type=text]` 选择器扩到 `textarea`(玻璃关 / 深色 / 深色焦点 + 主规则 + 焦点规则同步)+ `textarea{resize:none;line-height:1.35;min-height:44px;box-sizing:border-box}`;`autoGrowText()`(`height=auto`→读 `scrollHeight`>0 才设→`min(sh,104)px`)挂 input 事件 / `setMode("manual")` 显示 / `applyDefaultText` 末尾 / `renderCaptionChips`。**渲染层**:`mlLines(raw,max=3)`=split `/\r?\n/`+去首尾空白+丢空行+截断、`mlFlat(raw,sep)`=join;`drawManualText`(共享 ~19 款)取 mlLines、字号 `min(barH*0.34,barH*0.82/n/1.2)` 再按最宽行缩、`lineH=px*1.2` 底栏内垂直居中逐行画;`drawMinimalCorner` manual 取 mlLines、`lineH=px*1.4` 从 `h-pad` 往上叠、明暗采样区扩到整块、每行同色、auto 仍单行;`drawMagazineCover`/`drawMagazineLight`(同形 replace_all)第 1 行→标题(toUpperCase)其余→副标(join `"  ·  "`);`drawFootprint`/`drawBigDate`/`drawMinimalFrame` 改 `mlFlat`(`"  ·  "`/`" "`/`" "`)。自检 SYNTAX_OK、`mlLines`/`mlFlat` node 单测 9/9、加载期外链=NONE(运行期仅 api.openai.com+hm.baidu.com)。浏览器验(合成 1200×800 渐变照走真实 `#fileInput`):textarea + 样式共享、自增 65→87px、spy `fillText` 经典 y=835/875/914(distinctY=3)/ 右下角 722/758 / 杂志·浅 标题 910+副标 950、底栏像素扫出 2 条文字带(rows 19-63/79-118)、零报错。路线:§3.7 完成。
- **更早**:**v3.3.0**(**自定义 Logo / 签名 = 图片贴纸**——南心选「多行文字」+「自定义 Logo」并"接着做",两件偏设计她让我定方案。本版先做 **Logo**:**关键决策——不另起 logo 系统,而是给 beta.16 贴纸系统加 `type:"image"` 贴纸**,复用拖 / 缩放 / 删 / 选中框 / 导出全套基建。`#stickerActions` 加「＋ 图片 / Logo」按钮 + 隐藏 file input;`addStickerImage(file)`:FileReader→Image→**降采样 ≤600px**(离屏 canvas、保留透明 PNG alpha)→ `addSticker({type:"image",size:0.3,_img,_ar})`;`stickerMetrics` image 分支 `h=w/_ar`(保比例);`drawStickerLayer` image 分支 `drawImage`。**图片只端上读、绝不上传**。浏览器验证:蓝照贴红 logo → 中心采样 (220,40,40)红、角 (60,90,200)蓝、导出不 taint、零报错。**多行文字另起 v3.4**(要改各款画字方式)。`APP_VERSION=v3.3.0`、`sw.js` CACHE → `yinji-v44-2026-06-07-v3.3.0`。详见 `CHANGELOG.md`)
- **更早**(v3.3.0 细节):**自定义 Logo / 签名 · 路线图 §3.8 = 图片贴纸**——`#stickerActions` 加 `#stickerAddImage`「＋ 图片 / Logo」+ `#stickerImageInput`(hidden,accept image)。`addStickerImage(file)`:校验 + 上限 16 → `FileReader.readAsDataURL`→`new Image()`→onload 降采样最长边 ≤600px(离屏 canvas drawImage、保 alpha)→ `addSticker({type:"image",x:.5,y:.5,size:0.3,content:"",_img:离屏canvas,_ar:tw/th})`,各错误 toast 兜底。`stickerMetrics`:`type==="image"`→`w=size*base,h=w/_ar`(保原始宽高比)。`drawStickerLayer`:`else if image: if(_img) drawImage(_img,cx-w/2,cy-h/2,w,h)`;选中框 / 拖 / 缩 / 删 / `syncStickerEdit`(image 非 text 非 doodle→自动隐藏改字 / 颜色)全复用既有。接线:btn→`stickerImageInput.click()`,input change→`addStickerImage`。`_img/_ar` 运行时字段(贴纸不持久化,无序列化问题)。自检 SYNTAX_OK、符号齐、image 分支在、加载期外链=NONE。浏览器验证:版本 v3.3.0、按钮在;蓝照(60,90,200)贴红 logo(220,40,40)→ 中心 (220,40,40)红 logo / 角 (60,90,200)蓝照、`toDataURL` 不 taint(导出 OK)、console 零报错。路线:§3.8 完成;下一步 §3.7 多行文字(v3.4)。
- **更早**(v3.2.0):**文字颜色自适应 + 按样式记忆调色**——① `bgLumaAt` 读 canvas 区域均亮度(失败兜底 128);`drawMinimalCorner` 画字前采右下角落字区,`darkText=luma>145` 切深 / 白字 + 反色光晕。**只改 minimalCorner**(其余款已用渐变 / 底栏保证可读,不瞎动降回归)。② `STYLE_COLORS_KEY`/`styleColors`/`loadStyleColors`/`persistStyleColors`;`savePrefs` 末尾 `styleColors[state.style]={tone,toneAdj,filterId}`+持久化(所有调色改动已调 savePrefs,记忆自动累积);`recallStyleColor(style)` 校验后恢复 tone/toneAdj/filterId + `syncTonePad`/`markFilterActive`/`updateFilterActions`,无记忆则保持当前;`setStyle(style, recall)`,样式 chip click 传 `true`,`randomLook`/import/startup 用 `setStyle(id)` 不带 recall(隔离)。启动 `loadStyleColors()`。自检 SYNTAX_OK、符号齐、`setStyle(style, recall)`、chip 传 true、random 用不带 recall、加载期外链=NONE。浏览器验证:文字自适应(近白 photo 落字区 min 亮度 32 深字 / 近黑 max 246 白字)、按样式记忆(经典 复古·徕卡 黑白切回各恢复、🎲随机设新滤镜不被覆盖)、零报错。路线:§3.2 完成 + A2"按样式记忆"完成。
- **更早**(v3.1.1):**水印预设 +6(42 → 48) · 路线图 §3.1**——`SEASON` 加 `seaSalt`{bg:#EDF2F3}/`peach`{bg:#F8E7DC}/`greenGrape`{bg:#F0F1D6};`BRAND` 加 `wineRed`{bg:#1E1014,accent:#A23B4C}/`darkTeal`{bg:#0E1A1C,accent:#2E8C92}/`plumPurple`{bg:#17101E,accent:#7E4F9C}。`TEMPLATES`+6 `{id,name,desc,tags}`、`DRAW_DISPATCH`+6(→`drawSeasonStyle`/`drawBrandStyle`)、`CAT_IDS` season+3 / dark+3。`stylesForCat`/`renderStyleChips`/随机池/分享导入全自动纳入,无固定计数 / 硬编码 id。自检 SYNTAX_OK、6 新 id 各 ≥4 处、`TEMPLATES` 计数=48、加载期外链=NONE。浏览器验证(注入纯灰 photo):「水印」「全部」恰 **48** chip、6 新名全在、逐款底栏角像素与设计 bg **逐位一致**(海盐 237/242/243、蜜桃 248/231/220、青提 240/241/214、酒红 30/16/20、黛青 14/26/28、绛紫 23/16/30)、零报错。
- **更早**(v3.1.0):**拍摄地点 · 路线图 §3.5 安全那半步**——`readIFD` 加 `case 0x8825:out.__gps=get32(vo)`;`readTiff` 读 Exif IFD 后 `if(tags.__gps)readGpsIFD(...)`,返回条件加 `||typeof tags.lat==="number"`;`readGpsIFD` 读 `0x0001`latRef/`0x0002`lat/`0x0003`lonRef/`0x0004`lon,`gpsDMS` 度分秒→十进制,S/W 取负 + 范围校验 → `out.lat/lon`(JPEG+HEIC 共用 readTiff 两路都支持)。`gpsString(ex)`→`31.2400°N 121.4900°E`。`state.locationText:""`(新增);`autoFields` 加 `location:false`;设置页 `<input data-f="location">地点`(syncSettingsUI / change 处理器已对 autoFields 泛化、无需改)。`buildInfo` 末尾 `if(af.location){var loc=(state.locationText||"").trim()||gpsString(ex);if(loc)parts.push(loc);}`——骑 params 行零改 draw。auto 面板 `#locationInput`(全局 input 样式、无新 CSS)+ `syncLocationInput()`(renderAutoPanel 末尾调、按 location 切 hidden + 回填)+ input 监听→`state.locationText`+auto 重绘。**默认关 + 按"去灰字"偏好不加提示文字**(仅 placeholder)。`APP_VERSION=v3.1.0`、`sw.js` CACHE → `yinji-v41-2026-06-07-v3.1.0`。自检:inline `<script>`(1 块、**160216** 字符)SYNTAX_OK、`readGpsIFD`/`gpsDMS`/`gpsString`/`locationInput`/`syncLocationInput`/`locationText`/`case 0x8825`/`location:false` 全在、**加载期外链=NONE**。**Node 逐位验 GPS**(31°14′24″N 121°29′24″E→31.24/121.49、gpsString 正确、S/W 取负 −31.24/−121.49)+ **浏览器验**(版本 v3.1.0、「地点」复选框默认未勾、`#locationInput` 默认隐藏、toggle location → localStorage `autoFields.location` true/false 正确且其它字段不变、console 零报错)。**路线图 §3.5 完成**;GPS→地名(联网)留 4.x。
- **更早**(v3.0.1):**接入访问统计(百度统计)**——南心:"先加一个能看到有多少人用了的东西,只有我后台能看"。这是 App **第一个对外发信号**的功能(数跨设备人头必须靠外部聚合器,本 App 无后端)。给三条路她选「隐私友好轻量」;踩坑:GoatCounter 国内被墙、51.la 误建成移动 App 统计,最终用**百度统计**(国内稳、专做网站)。`loadAnalytics()` 运行时(`load` 后 `setTimeout`)异步注入 `hm.js`、**本地 / 预览不计、断网静默、绝不发照片**;仍**零外链(加载期)**——无静态 `<script src>`,同 AI fetch 一性质。`.privacy-note` 补一句如实告知(可能用 cookie、只记匿名访问量、不涉及照片);**照片本地处理铁律不变**。站点 ID `0efa02b...` 是公开标识非密钥故 Claude 写入。后台在 `tongji.baidu.com` 看,**需真实部署 + 访问才计数**。详见 `CHANGELOG.md`)
- **本轮新增**(v3.0.1):**接入访问统计 · 百度统计**——`var ANALYTICS_BAIDU_ID="0efa02b688603a4b1765cdbfc502de6e"`(置 `APP_VERSION` 旁;留空=不统计);`loadAnalytics()`:`!ID`→跳、`localhost`/`127.0.0.1`/`file:`→跳(本地预览不计)、`window._hmt=window._hmt||[]`(百度需全局)、`createElement("script")`+`async`+`src="https://hm.baidu.com/hm.js?"+ID`+`insertBefore` 首个 script 前、`try/catch` 静默;`readyState==="complete"?setTimeout:onload→setTimeout`(load 后再注入、零首屏阻塞)。退役 GoatCounter 占位(删 `ANALYTICS_CODE`/`pingAnalytics`,grep 残留=0)。`.privacy-note` 补如实告知句。`APP_VERSION=v3.0.1`、`sw.js` CACHE → `yinji-v40-2026-06-07-v3.0.1`(SW 对跨域 `return`,hm.baidu.com 不进缓存)。自检:inline `<script>`(1 块、**157927** 字符)`node --check`=SYNTAX_OK、goatcounter 残留=0、**加载期外链=NONE**、运行时外链=api.openai.com+hm.baidu.com。浏览器验:版本 v3.0.1、`ai.list()`=4、**localhost 守卫生效(未注入 baidu 脚本、`_hmt` 未设)**、src 构造正确、console 零报错。**待南心部署后看数**;更干净方案可换"自建计数"。⚠️ 隐私:百度统计确用 cookie / 收集访客信息,不夸大;**照片绝不上传不变**。**【v3.8.0 期间更新】`ANALYTICS_BAIDU_ID` 已由 `0efa02b…` 换成新站点 `f488ac0d27fb6dbe03580f6375784d1e`**(南心为迁 Netlify 在百度统计新建了站点;老站点数据停在那、不与新站点合并;新 ID 随 v3.7/v3.8 部署后生效)。
- **更早**(v3.0.0 正式版):**beta → 正式版收口(#165 清死代码 + #166 横屏安全区 + #168 版本转正)**——南心:"接下来你规划一下正式版的开发",AskUserQuestion 确认 = **直接稳定收口转正**(她已自行真机验证 beta.18)。① **清死代码 `#searchOverlay`(#165)**:beta.9 删 `searchBtn` 入口后整套弹窗不可达、休眠至今,本版彻底移除——删 HTML 弹窗块(含 `#searchInput`/`#catRow`/`#newCustomBtn`/`#importTplBtn`/`#searchResults`/`#closeSearch`)、`openTemplatePicker`/`renderCatRow`/`renderSearchResults` 函数及监听、9 个死变量(保留同行活变量 `toastEl`)、死 `CATEGORIES` 数组(**`CAT_IDS` 是活的保留**;`STYLE_/FILTER_CATEGORIES` 是内嵌页活数据保留)、`#newCustomBtn`/`#importTplBtn` 死 handler;**3 个活函数里删对已不可达弹窗的死调用(行为等价)**:`deleteCustomTemplate` 删 `renderSearchResults(searchInput.value)`(上方 `renderStyleSeg()` 已刷新)、`shareCustomTemplate` 删 `searchOverlay.classList.remove("show")`、`importTemplateObj` 删同款。grep 死符号残留=0。② **横屏左右安全区(#166)**:`body` 横向 padding 补 `env(safe-area-inset-left/right)`(`viewport-fit=cover` 配套,上下早已做);竖屏 / 非刘海 inset=0 逐像素一致;Dynamic Type / 横屏不错位经评估本布局(居中 480px 窄列)已天然处理,不为非问题硬造改动。自检:抽 inline `<script>`(1 块、**156760** 字符,较 beta.18 少约 4.5KB)`node --check`=**SYNTAX_OK**、外链 0、`APP_VERSION=v3.0.0`;`sw.js` CACHE → `yinji-v39-2026-06-07-v3.0.0`。**全量回归自检(浏览器,清 SW 缓存后)**:版本 v3.0.0、`ai.list()`=4 含 scene、死弹窗 / 死按钮 DOM 全 gone、内嵌活按钮全在、**四条自定义水印活路径逐条验过**(新建建出 `custom_*`、分享出 `YINJI1:` 码 175 字、删除回 `classic` + 空态提示、导入弹窗开)、滤镜 33 chip、贴纸 + 20 emoji、3 个 AI 按钮、`canvas.toDataURL` 出有效 PNG、`body` 安全区 padding 解析 0px 无溢出、console **零报错**。**v3.0.0 正式版就绪,待负责人部署**;后续待南心定(管家 #153/#154 图四 / 图五阻塞待补图、路线图 3.2 文字颜色自适应 / 3.5 读 GPS + 手填地点 / A2 按样式记忆调色 / Phase B 联网增强,按需排 v3.0.x / v3.1)。
- **更早**(v3.0.0-beta.18):**A4 场景识别 → 推荐水印样式(#152,南心三件事第 3 件 Task 3 收尾,放最后做)**——南心:"3.完成管家给你的更新内容 完成 3.0.0 beta 版本的全部开发",管家 #152"再加一个 AI 功能(放最后做)"。按 `roadmap-3.0.md` Phase A 落地 **A4**(A1 配色 beta.1 / A3 文案 beta.7 之后,Phase A 最后一块)。① **数据**:`SCENE_STYLES` 5 场景各一组样式 id(landscape 暮山/极简右下角/签到打卡/节气·青/雾蓝、night 黑底白字/暗夜蓝/电影黑边/杂志封面、food 宝丽来完全款/奶茶/柯达金/玻璃卡片、portrait 极简框/莫兰迪/拍立得/复古胶片、daily 经典/玻璃底栏/燕麦/极简右下角)+ `SCENE_LABEL` + `SEASON_STYLE`(春夏秋冬→springCherry/summerSea/autumnMaple/winterSnow)+ `styleNameOf()` 查 `TEMPLATES`。② **采样** `analyzeScene(bmp)`:一遍 48×48 `getImageData` 出 `luma`/`warmth=avg(r-b)`/`greenFrac`(g>r+8&&g>b+8)/`blueFrac`(b>r+8&&b>g+6),**聚合不可还原**。③ **规则** `classifyScene()` 短路优先级:`luma<58||time==="夜"`→night、`green+blue>=0.42||lens==="广角"`→landscape、`bigAperture&&warmth>10&&nature<0.22&&luma>95`→food、`bigAperture&&lens!=="广角"`→portrait、`warmth>16&&nature<0.16&&luma>120`→food(无 EXIF 兜底)、else daily;`suggestStyles()` 取列表 + 季节 unshift + 去重取 4。④ **交互/接线**:`#styleBlock` 加 `#sceneRecBtn`(`.cap-spark`)+ `#sceneList`(`.cap-list`,**复用 A3 类零新 CSS**);`aiScene()` 无图拦截→渲染 chip(点击 `setStyle` + toast);`register({id:"scene",onDevice:true,run:()=>suggestStyles()})`;`getContext()` 加只读 `scene`(不含像素/GPS)。自检:抽 inline `<script>`(1 块、**161230** 字符)`node --check`=**SYNTAX_OK**、外链 0、A4 全符号 + 注册 + getContext + 接线 + HTML 齐、`APP_VERSION=v3.0.0-beta.18`;`sw.js` CACHE → `yinji-v38-2026-06-07-v3.0.0-beta.18`。**浏览器预览验证**(清 SW 缓存后):`ai.list()`=4 含 scene;注入 5 纯色经 `ai.run("scene")` 分类**逐一命中**(绿/蓝→风景、暗→夜景、暖亮→美食、中性→日常,`getContext().scene` 一致);点按钮渲染 4 chip、点 chip 套样式(state.style classic→glassBar、`#styleSeg` 高亮)、console 零报错。**#152 完成 → 南心三件事全部完成 → v3.0.0 beta 全部开发收口** 🎉(Phase A 三端上 AI 齐活 + B1 联网)。
- **上一版**(v3.0.0-beta.17):**滤镜预设 20 → 32 款(#151,南心三件事第 3 件 Task 3 之一)**——南心:"3.完成管家给你的更新内容 完成 3.0.0 beta 版本的全部开发",管家 #151"增加更多滤镜预设"。**只改两处数据**:`TONE_LIST` +12 行 `{id,name,cat}`(按分类分组插入,"全部"视图仍成块)、`TONE_PARAMS` +12 组参数(全落在既有 20 款取值范围:sat 0–1.35 / con 0.86–1.24 / bri 0.95–1.08 / temp −28–32 / tint −5–11 / lift 0–30)。新增:`slide`反转片{sat:1.18,con:1.10} / `oldPhoto`旧时光{sat:0.68,lift:28} / `bwSepia`棕调{sat:0,temp:20,tint:7} / `bwCool`冷黑白{sat:0,temp:−16,tint:−4} / `honey`蜜糖{temp:20,lift:10} / `terracotta`赤陶{temp:24,tint:9} / `nordic`北欧{sat:0.90,temp:−12,lift:12} / `teal`蓝青{temp:−12,tint:7} / `sakura`樱花{tint:−5,bri:1.08} / `mint`薄荷{temp:−6,tint:9} / `morandi`莫兰迪{sat:0.60,lift:16} / `dreamy`梦幻{con:0.90,lift:26}。**零回归**:`applyTone`/`effectiveParams`/`gradedBitmap`/`getSize`/chip 渲染(`TONE_LIST.forEach`)/ 分类过滤(`t.cat`)/ 随机池(`TONE_LIST.forEach push`)/ 自定义滤镜 `base` 校验全自动兼容新 id,无固定计数 / 硬编码 id 列表需同步。自检:抽 inline `<script>`(1 块、**157754** 字符)`node --check`=**SYNTAX_OK**、外链 0(仅 1 处 http 为 AI 地址 placeholder)、12 新 id 在 `TONE_LIST`+`TONE_PARAMS` 均在、`APP_VERSION=v3.0.0-beta.17`;`sw.js` CACHE → `yinji-v37-2026-06-07-v3.0.0-beta.17`。**浏览器预览像素级验证**:注入纯色 photo(120,140,160)→ 「滤镜」页 `#toneSeg` 恰 **33** chip(原图 + 32)、12 新名按分类序全在、点 6 款采像素与手算逐位吻合(反转片 115/141/171、棕调 165/151/123、冷黑白 122/135/156、樱花 146/153/170、莫兰迪 142/150/157)、各异且合预期、console 零报错。**#151 完成**;3.0.0 beta 仅剩 **#152 再加一个 AI(放最后做,beta.18)**即全部开发收口。
- **更早**(v3.0.0-beta.16):**编辑器第三标签「拼贴」→「贴纸」(#164,南心三件事第 2 件)**——南心:"2.拼贴改成贴纸 功能就是添加贴纸,没有拼图了"。管家口径:「贴纸贴任意照片、可拖动、emoji + 手绘 + 文字」。beta.15 已把**拼图入口**并进主卡片(多选 2–9 张),故本版把编辑器第三标签从「拼贴」改「贴纸」——任意单张 / 拼好的图都能贴;**拼图能力没删**(主入口多选仍拼图,拼好落「贴纸」标签、`#stickerLayoutRow` ≥2 张才显示、可继续换版式)。① **改名 + 面板重排**:`#editTabs` 第三按钮 `data-tab="collage">拼贴`→`data-tab="sticker">贴纸`;`setEditTab` 白名单 `if(tab!=="color"&&tab!=="sticker")tab="watermark"`,进 sticker 调 `refreshStickerPane()`;`#stickerPane` = `.tone-head`(贴纸)+ `#stickerLayoutRow`(原 `#collageLayouts` 版式)+ `#stickerCatSeg`(emoji/doodle)+ `#stickerPalette` + `#stickerActions`(`#stickerAddText`)+ `#stickerDecoEdit`(`#decoSmaller`/`#decoBigger`/`#decoEditText.deco-text-only`/`#decoColorBtn.deco-colorable`+`#decoColorInput`/`#decoDelete`)+ `.sticker-tip`;`startCollageFromFiles` 末尾 `setEditTab("sticker")`。② **数据 + 图层**:`state.stickers` 数组,项 `{type:'text'|'emoji'|'doodle',x,y,content,size,color}`(`x/y` 归一 0–1、`size` 相对 `Math.min(canvas.w,canvas.h)`),`addSticker()` 懒初始化 + 上限 16,`resetStickers()` 清空 + 复位;`stickerMetrics()` 算像素,`drawStickerLayer()` 遍历:text=半透明黑描边+实色 fillText、doodle=`drawDoodle()`、emoji=fillText,选中**仅** `!_exportClean&&editTab==="sticker"&&i===stickerSel` 时画白虚线框;`draw()` 末尾 `drawStickerLayer()`(永远压最上)。③ **手绘零依赖**:`DOODLE_IDS=[heart,star,sparkle,sun,cloud,flower,leaf,bubble,check,crown,moon,bolt]`,`drawDoodle(g,id,cx,cy,half,color)` 单位空间 [-1,1] canvas 路径、可染色;选盘手绘 chip 用 34×34 离屏 canvas 现画、emoji chip 直接 textContent——不引图片 / 字体 / SVG 外链。④ **交互**:主 canvas 指针事件仅贴纸标签 + 有图时生效(`pointerdown` 开头 `if(editTab!=="sticker"||!state.bitmap)return`),`stickerHit()` 选中起拖、`pointermove` 改 x/y(clamp 0–1)、`endStickerDrag` 收尾;`begin()` 长按看原图在贴纸标签命中贴纸则 `return`(避冲突);编辑条调 `size`(小 / 大)、改 `color`(文字 / 手绘可染,`syncStickerEdit` 切 `.deco-colorable`)、`#decoEditText`(仅文字 `.deco-text-only`)prompt 改字、删除;`#stickerAddText`→`addStickerText()`(prompt ≤30 字)。⑤ **导出**:`exportDataURL(type,q){_exportClean=true;draw();var url=...toDataURL...;_exportClean=false;draw();return url;}`(同步无闪烁,贴纸烤进、选框被压),4 个导出位(png/jpeg buildExport、saveBtn jpeg 0.92、copyImgBtn png)全改走它。自检:抽 inline `<script>`(1 块、**156285** 字符)`node --check`=**SYNTAX_OK**、外链 0(仅 1 处 http 为 AI 地址 placeholder)、`drawStickerLayer`/`drawDoodle`/`addSticker`/`startCollageFromFiles`/DOM id 配平齐、`APP_VERSION=v3.0.0-beta.16`;`sw.js` CACHE → `yinji-v36-2026-06-06-v3.0.0-beta.16`。**浏览器预览已用像素级断言全程验过**(加 / 改色 / 缩放 / 删除 / 文字 / 拖动 / 导出干净度 194px 选框被压 / 拼图合成 + 落贴纸标签 + 切版式重合成 + 贴纸烤在拼图上);**真机交互待南心 iPhone 验**;**#164 完成,Task 2(拼贴→贴纸)收口**,3.0.0 beta 还剩 Task 3:#151(更多滤镜预设,纯数据)、#152(再加一个 AI,**排最后**)。
- **更早**(v3.0.0-beta.15):**拼图入口改造(#163,南心三件事第 1 件)**——南心:"1.我要在这个界面就能一次最多选择 9 张图片进行拼图,删掉底下的"。AskUserQuestion 确认 = **并进主入口**(主卡片选 1 张走普通编辑、2–9 张自动拼图,删底部按钮)。① **主入口合并**:`#fileInput` 加 `multiple`;startView 文案改「选 1 张直接编辑,选多张(最多 9 张)自动拼成一张」;`fileInput.change` 按张数分流——`>=2` 清 `value` 后走 `startCollageFromFiles(files)`,`===1` 走原 `loadFile()` 单张路径,`===0` 提示「没选到照片」。② **删底部独立拼贴入口**:删 `#collageEntry` `<button>` + 其 `.collage-entry` CSS + `var collageEntry` + `collageEntry.addEventListener`(自检 `collageEntry` 残留=0);抽共享函数 `startCollageFromFiles(files)`(IIFE 内函数声明提升):`<2` 张 toast 拦截、`>9` 张 `slice(0,9)`+提示、`Promise.all(files.map(loadBitmap))` 读图过滤无效项、`ok.length<2` 兜底,成功则 `collagePhotos=ok; collageLayoutId=null; resetCollageDecos()` → 切编辑器 → `applyCollageToMain()`→`setMode("manual",true)`→`renderAutoPanel()`→`setEditTab("collage")`。编辑器「拼贴」标签 / `#collageInput` 保留(beta.16 改造对象)。③ **布局表扩到 9 张**:`COLLAGE_LAYOUTS` 加键 5–9(`cells` 已归一化、每布局 `cells.length` 等于照片数,已逐一核对):`5`=上 2 下 3 / 左大右 4、`6`=3×2 / 2×3、`7`=上 3 下 4、`8`=4×2、`9`=3×3;`collageLayoutsFor`/`drawCollageInto`/`applyCollageToMain` 既有逻辑无需改,渲染管线(烤 `collageCanvas` 1500²→`state.bitmap`→滤镜 / 水印 / 导出)不变。自检:抽 inline `<script>`(1 块、**150308** 字符)`node --check`=**SYNTAX_OK**、外链 0(仅 1 处 http 为 AI 地址输入框 placeholder)、`startCollageFromFiles`/`#fileInput multiple`/`COLLAGE_LAYOUTS 5–9`/`stage-hint` 齐、`collageEntry` 残留=0、`APP_VERSION=v3.0.0-beta.15`;`sw.js` CACHE → `yinji-v35-2026-06-06-v3.0.0-beta.15`。**真机交互(多选 2–9 张 / 各布局成图 / 单张仍走普通编辑 / 拍照入口)待南心 iPhone 验**;**#163 完成**,下一步 #164 贴纸(beta.16)。
- **更早**(v3.0.0-beta.14):**联网 AI 文案 B1(#146,新功能)**——南心:"ai 继续做把。…『长按图片可看原图对比』不要去掉"。做两件:① 落地 #146;② **保留** `.stage-hint`「长按图片可看原图对比」(了结一直挂着的"那行小灰字删不删"——**结论:保留**,自检 `STAGE_HINT_PRESENT=true`)。**方案 provider-agnostic BYOK**:设置里粘 API 地址(base)+模型+自己的 Key,任何 OpenAI 兼容 `chat/completions` 服务可用(不锁死一家、不替南心选型);`aiEndpoint()` 归一 base(已 `/chat/completions` 原样用、否则补)。**复用扩展点**:`window.Yinji.ai.register({id:"captionOnline",onDevice:false,run:()=>aiCaptionOnline()})`,`ai.list()`→`["smartColor","caption","captionOnline"]`。**只发文字**:`aiContextLine()` 把 `getContext()`(只读快照,无像素 / 无 GPS 原始坐标)转成中文描述串(季节 / 时段 / 暖冷 / 明暗 / 浓淡 / 焦段 / 设备 / 已有文字)。**UI**:`#captionBtn`(端上,不变)后加 `#captionAiBtn`(🌐,仅 `settings.aiOnline` 真时显示,`applyAiOnlineUI()` 切 `.hidden`,同切设置页 `#aiOnlineFields`)。**`aiCaptionOnline()`**:`aiOnlineReady()` 校验地址 / 模型 / Key 三件齐 → `_aiConsentSession` 当次授权(每次开 App 头一回 `confirm` 把将发的文字摊给她看)→ `#captionList` 显 `联网生成中…` → `fetch` POST(`Authorization: Bearer`,`AbortController` 20s 超时)→ 取 `choices[0].message.content` → `parseAiCaptions`(剥编号 / 项目符号 / 引号、长度 ≤24、去重取前 4)→ `renderCaptionChips`;**没填好 / HTTP 非 2xx / 网络 / CORS / 超时 / 空** 一律 `catch` → `renderCaptions()` 端上兜底 + 老实 toast(点名"网络问题或服务商不允许网页直连")。**AI 绝不作为前置**:端上「✨ 来点灵感」原样在。重构 `renderCaptions()`=`renderCaptionChips(suggestCaptions())`,联网成功 / 失败两路共用 chips 渲染。**设置**:`defaultSettings()` 加 `aiOnline:false / aiBase / aiModel / aiKey`(`loadSettings` 通用分支天然兼容,老存档缺键保留默认);占位"AI · 即将推出"**换成**"AI · 联网文案"(开关 `#aiOnlineToggle` + 折叠 `#aiOnlineFields` 三输入,Key `type="password"`+`autocomplete/autocapitalize/autocorrect=off`、**不在任何会提交的 form 内**、只写 localStorage);**代码 Claude 不填任何 Key**——只搭输入框,值南心自己粘;`syncSettingsUI` 回填 4 控件 + `applyAiOnlineUI`,启动处 `applyLongPressHint()` 旁加 `applyAiOnlineUI()`,`恢复默认设置` 会清回默认(含清空 Key)。**退役死代码**:删占位按钮 `#aiSoonBtn` 的 HTML+JS(`AISOON_RESIDUE=false`)。自检:抽 inline `<script>`(1 块、**148687** 字符)`node --check`=**SYNTAX_OK**、外链 0、`B1_TOKENS_MISSING=none`、`STAGE_HINT_PRESENT=true`、`AISOON_RESIDUE=false`;`sw.js` CACHE → `yinji-v34-2026-06-06-v3.0.0-beta.14`。**真机交互(联网调用 / CORS 真实表现 / 当次授权弹窗 / 相册)待南心 iPhone 验**;**#146 完成**。
- **更早**(v3.0.0-beta.13):**拼贴升为编辑器第三个标签(#145,新功能)**——南心:"接下来你做拼贴标签,同时我希望在做水印和滤镜的时候也能拼图",并确认 ① **先做拼贴、再做 AI**、② **三个标签随便切、互不影响**。把 beta.3/beta.6 的"一次性确认弹窗"拼图(`#collageOverlay`:选图→布局→加字贴纸→点「用这个拼」一锤定音烤进 `state.bitmap`、之后状态丢失)**改造成与「水印 / 滤镜」并列的常驻第三标签**(Design A:内嵌实时重合成,退役旧弹窗)。① **标签栏** `#editTabs` 2→3 个 tab(`data-tab` watermark / color / collage;滤镜 tab 内部值仍 `"color"`);`setEditTab` 扩三态、切 `#watermarkPane`/`#colorPane`/`#collagePane`、切到拼贴调 `refreshCollagePane()`。② **实时重合成**:`applyCollageToMain()` 复用**同一**离屏 `collageCanvas`(1500²)→ `drawCollageInto(...,-1)`(成图不含选中框)→ `state.bitmap=collageCanvas; state.exif=state.exifApp1=null` → **`invalidateGrade()`** → `draw()`;**踩坑**:`_gradeCache` 以 `src===bmp` 同对象引用为键,复用同一 canvas 只改像素**必须** `invalidateGrade()` 否则滤镜拿陈旧缓存。`syncCollage()`=刷预览 + commit 大图,离散操作(换布局 / 加字贴纸 / 小 / 大 / 改文字 / 改色 / 删除)走它;拖动 pointermove 只刷 600 预览、`endCollageDrag` 松手才 commit(避免每次 move 重合成 1500² + 重影调)。水印 / 滤镜本就叠在 `state.bitmap` 上,故三标签天然共存。③ **新 `#collagePane`**:空态 `#collageEmpty` + 编辑态 `#collageEditWrap` 互斥;`clearCollageState()` 挂 `loadFile`/`resetBtn`,单图流程切拼贴回空态。④ **退役 `#collageOverlay`**:整块删弹窗 HTML(防 id 重复)+ 删 `collageOverlay`/`collageConfirm`/`collageCancel` var / 监听 + 旧 `closeCollage()`(防 ReferenceError),grep 0 残留;新增 scoped `.collage-edit.hidden`/`.collage-empty`/`.collage-empty.hidden`/`.collage-empty-tip` CSS。自检:抽 inline `<script>`(1 块、**143777** 字符)`node --check`=**SYNTAX_OK**、外链 0、15 个 collage 相关 id 各唯一、`#editTabs` 3 tab;`sw.js` CACHE → `yinji-v33-2026-06-06-v3.0.0-beta.13`。**真机交互待南心验**;**AI 文案 B1(#146)按"先拼贴后 AI"推迟下一版**;`.stage-hint` 仍未动。
- **更早**(v3.0.0-beta.12):**修 bug + UI 调整(纯 UI,无新功能)**——南心发图问"存为滤镜在哪里"。① **「＋ 新建我的滤镜」改常驻显示(#158)**:`updateFilterActions()` 原用 `canSave = hasGrade() && !state.filterId` 把 `#saveFilterBtn` 条件隐藏(只有调过效果、且不在已存滤镜上时才出现)——南心停在「原图」就找不到;改成 `saveFilterBtn.classList.remove("hidden")` **永远显示**,与「水印」页 `#newTplInline` 对齐。点击逻辑不变:`openFilterSave()` 首行 `if(!hasGrade())` 原本就有引导 toast「先选个滤镜或拨动调色盘,有效果了再保存」,以前按钮被藏点不到、现在常驻就能触发。`#shareFilterBtn`/`#delFilterBtn` 仍按 `state.filterId` 显隐。② **「🎲 随机一套」做成全局(#159,南心第 5 点)**:新增 `.edit-tabs-row`(flex)包住 `#editTabs`(水印 / 滤镜分段)+ `#filterRandBtn`,把随机按钮从 `#colorPane > .tone-head` 移到标签栏右侧,**任意标签可点**;`#filterRandBtn` id / 事件 / `randomLook()`(随机水印 + 滤镜)零改动,`#editTabs` 仍只含 2 个 tab(`setEditTab` 的 `querySelectorAll("button")` 不受影响);`.tone-head` 去随机按钮后「调色盘 ▾」自带 `margin-left:auto` 仍靠右。CSS 仅加 `.edit-tabs-row`、`.edit-tabs{flex:1 1 auto;min-width:0}`、`.filter-rand{flex:0 0 auto;white-space:nowrap}`。自检:抽 inline `<script>`(1 块、143259 字符)`node --check`=SYNTAX_OK、外链扫描 0、`#filterRandBtn` 全文唯一(移动非复制);`sw.js` CACHE → `yinji-v32-2026-06-06-v3.0.0-beta.12`。**未动** `.stage-hint`「长按图片可看原图对比」(仍等南心定夺)。
- **更早**(v3.0.0-beta.11):**清理 + 改叫法(纯减法,无新功能)**——南心再次当面点名 beta.10「只清了编辑器、设置里没清」,这版按「全部清除」把设置页灰字扫净 + 两处改名。① **设置页灰字全清(#155)**:删 `.set-hint`×5(外观 / 默认打开水印 / 自动信息 / 导出尺寸 / AI 预告各一段)+ 删每个开关项标题下的 `.setting-hint`×3(液态玻璃 / 记住上次选择 / 长按看原图);`.privacy-note` 由长段收成一句「照片只在你的设备本地处理,绝不上传任何服务器。」(**隐私铁律"明确告知"故保留不删,只精简**);`.set-hint`/`.setting-hint` 两条 CSS 在用法全删后已无引用,一并移除。**刻意不动** `.stage-hint`(预览图上方「长按图片可看原图对比」,在编辑器非设置页、且是长按看原图功能提示,本轮范围外,已在用户版日志问南心是否也去掉)。② **「调色」标签→「滤镜」(#156)**:仅改 `#editTabs` 里 `data-tab="color"` 按钮的**显示文字**「调色」→「滤镜」,**内部值 `"color"` 不变**(`setEditTab`/`state.editTab` 全照旧),改完标签「水印 / 滤镜」与各页区标题对齐;「调色盘 ▾」是独立微调盘,名字不动。③ **「＋ 存为滤镜」→「＋ 新建我的滤镜」(#157)**:`#saveFilterBtn` 文字 + `#filterMineHint` 空态里的引用同步改(与「水印」区「＋ 新建我的水印」对齐);**按钮 id / 点击逻辑 / 保存弹窗标题「存为我的滤镜」/ 保存 toast 不动**(南心确认"改名就行")。自检:抽 inline `<script>`(1 块、143318 字符)`node --check`=SYNTAX_OK、外链扫描 0、`set-hint`/`setting-hint` grep 0 残留、`data-tab="color"` 仍在;`sw.js` CACHE → `yinji-v31-2026-06-06-v3.0.0-beta.11`。
- **更早**(v3.0.0-beta.10):**清理 + 改叫法(纯减法,无新功能)**——南心当面逐条点了几处「之前让改没改干净」的:① **去灰色说明小字(#144)**:`#captionBtn`/`#smartColorBtn` 去掉内层 `.sub`(只剩「✨ 来点灵感」「✨ 帮我配色」)、删调色盘下 `.dial-hint` 那行;连删 3 条只服务这些小字的 CSS(`.dial-smart .sub`/`.cap-spark .sub`/`.dial-hint`,`.sub` 类本身保留)。② **去小尾巴 + 清常用栏死代码(#149)**:删设置项 `#resetPinsBtn`「重置主样式条常用栏」+ 其监听 + var 引用;**整套 segPins 全删**(`SEG_PINS_KEY`/`segPins`/`defaultPins`/`loadSegPins`/`saveSegPins`/`isPinned`/`addPin`/`removePin`/`togglePin`——beta.9 起常用栏已不在主界面渲染,均无人调用);清各调用点(`renderSearchResults` 的 pin 按钮、`deleteCustomTemplate`/`customSave`/`importTemplateObj` 的 add/removePin、`clearTplBtn` 的 removePin 循环)+ 删死 CSS `.template-item .pin`;顺带 `clearTplBtn` toast / `resetSettings` 确认语 / 新手引导第 2 步去掉对常用栏的措辞。③ **「模板」→「水印」(#150)**:编辑器 `#styleBlock` 区标题「水印模板」→「水印」、`styleMineHint`「我的模板」→「我的水印」、`#newTplInline`「＋ 新建我的水印」、`#importTplInline`「导入水印」;新建 / 分享弹窗标题与字段、设置「默认打开水印」「清空我的自定义水印」、隐私说明「(水印 / 偏好 / 设置)」;JS 字符串里 `deleteCustomTemplate`/`openCustomEditor`/`customSave`/`shareCustomTemplate`/`importTemplateObj` 及 `openShare` 的 `noun`(`filter`→「滤镜」否则→「水印」)全部改口径。**刻意不动**:8 处源码注释里的「模板」、休眠的 `#searchOverlay`(「更多模板」弹窗,`searchBtn` 入口 beta.9 已删故整体不可达,留待与其一并清理)。自检:抽 inline `<script>`(1 块、143334 字符)`node --check`=SYNTAX_OK、外链扫描 0、被删符号 grep 0 残留;`sw.js` CACHE → `yinji-v30-2026-06-05-v3.0.0-beta.10`。
- **更早**(v3.0.0-beta.9):**水印模板区做成「和滤镜一样」的内嵌浏览 + 文案灵感升级**——南心当面指出「滤镜」页能分类浏览 chips、「水印」页却只有一个孤零零的「选择模板」按钮还要再开弹窗,故把**水印模板区照搬滤镜区**:① `#watermarkPane` 顶部用 `#styleBlock.tone-block` 装下「分类行 `#styleCats.filter-cats`(9 类:全部 / 经典 / 深色 / 彩色 / 季节 / 品牌 / 玻璃 / 节日 / 我的)+ 模板 chips `#styleSeg.seg.tone-seg` + 空态提示 `#styleMineHint` + 动作行 `#styleActions`(新建 / 导入 / 分享 / 删除)」,**全程复用滤镜区现成 CSS 类、零新增样式**,故视觉与滤镜区逐像素一致。② 模块级 `styleCat`(默认 all)+ `renderStyleCats()`/`stylesForCat()`/`renderStyleChips()`/`markStyleActive()`/`updateStyleActions()`;`renderStyleSeg()` 收敛为前两者别名,既有 7 处调用点照常工作(低回归)。③ 删头部 `#searchBtn`「更多模板」放大镜(头部只剩齿轮);旧 `#searchOverlay` 弹窗**保留但休眠**(已无入口,留着免动其多处 `renderSearchResults` 调用点,后续单独清理)。④ 内嵌动作按钮取新 id(`#newTplInline`/`#importTplInline`/`#shareTplInline`/`#delTplInline`)避免与休眠弹窗的 `#newCustomBtn`/`#importTplBtn` 撞;新建保存 & 导入后跳 `styleCat="mine"` 并提示「在『水印』页『我的』分类里」。⑤ **文案灵感**(「✨ 来点灵感」)由 beta.7 的「4 种固定句式各产一句」升级为**15 个词库 + Fisher-Yates 洗牌去重取 4**(情境词优先、通用词兜底、无 EXIF / 无照片也有 4 条、每点一次换一批),仍纯端上不联网。⚠️ **已知陈旧项**:设置面板「重置主样式条常用栏」现已无可见效果(主样式条 segPins 不再显示在「水印」页),待后续(#144 周边)清理。浏览器预览验证:头部只剩齿轮、`#styleCats` 9 类、`#styleSeg`=`seg tone-seg`、全部=42 chips、点「深色」筛 5 条、点 chip 应用 + 高亮、内置时分享 / 删除隐、「我的」空态出提示、新建后跳「我的」并出分享 / 删除,console 零报错。仍单文件 + 零外链;`node --check`=SYNTAX_OK、外链扫描 0 命中;`sw.js` CACHE 升到 `yinji-v30-2026-06-05-v3.0.0-beta.9`。
- **更早**(v3.0.0-beta.8):**编辑器「水印 / 调色」分页 + 函数体注释清理 + UI 简洁化**——6/5 冲刺三项 🔴 全做完。① **调色 / 水印分离**:进编辑器顶部加「水印 / 调色」两个标签(`#editTabs`),`state.editTab` 默认 `"watermark"`,`setEditTab(tab)` 只切两个 `.edit-pane`(`#watermarkPane` 装选模板 + 手动 / 自动文字,`#colorPane` 装滤镜 + 调色盘 + 帮我配色 + 来点灵感)的 `.hidden` 显隐、并给当前标签加 `.on`(用 `--accent` 点亮)。**设计契约:只切 DOM 显隐,绝不动渲染管线——`#canvas` 始终同时带水印 + 调色,导出一字未改**;开新照片(`setMode`)/ 拼图 confirm(`resetCollageDecos`)后都复位到「水印」页。实测切到「调色」页导出,同一像素仍为 `[107,107,107]`(与分页前逐位一致),证明成图零变化。② **函数体注释清理**:用正则感知的分词器删掉函数体内 448 条解释性注释、保留 45 条结构横幅;**只动注释,代码一字未改**——以「去掉所有注释 + 空白」的代码骨架比对,新旧文件骨架逐字节相同(112293 字符一致)。**⚠️ 这里清的是代码注释(程序员才看的绿字),不是删设置 / 删功能**;设置模块(齿轮入口 + 外观 / 打开照片时 / 自动信息 / 照片与导出 / 数据与隐私 / 关于六大分区)与 App 用法、导出全部完整保留(grep 设置相关标识符 52 处全在)。③ **UI 简洁化**:分页后界面更干净,当前标签 `--accent` 点亮一眼看清在哪页。浏览器预览验证:`#appVer`="v3.0.0-beta.8"、`editTabs` 水印(on)/ 调色(off)、`watermarkPane` 显 / `colorPane` 隐、`ai.list()`=`["smartColor","caption"]`、console 零报错。仍单文件 + 零外链。`sw.js` CACHE 升到 `yinji-v29-2026-06-05-v3.0.0-beta.8`。
- **更早**(v3.0.0-beta.7):**智能文案灵感 A3(端上规则版,3.0 第二个 AI 功能)**——6/4 冲刺「在 A1 基础上继续加 AI 能力」第⑦项。手动输入文字框(`#textInput`)下加「✨ 来点灵感」按钮 `#captionBtn` + 候选区 `#captionList`;点一下经 `suggestCaptions()` 读这张照片的**端上线索**生成 2~3 个文案 chip,点 chip 把文案填进文字框(`textInput.value=...`→`setMode('manual')`→`draw()`)、可再手改,再点一次换一批。线索来自 ① `exifClues(state.exif)` 从 EXIF 拍摄时间正则解析出**时段**(晨 / 上午 / 午 / 午后 / 暮 / 夜)+ **季节**(春 / 夏 / 秋 / 冬)+ 设备 + 大光圈 / 焦段,② A1 的 `state.toneAdj` 冷暖 / 明暗倾向。`suggestCaptions()` 按线索拼 4 种句式 + 6 条兜底、去重后取前 3。**纯端上、零网络、不读原始像素、不读 GPS、不下载模型**。已注册为 `window.Yinji.ai` 第二个功能(`{id:'caption',label:'文案灵感(端上)',onDevice:true,run}`);`getContext()` 扩展只读快照增 `hasExif/timeOfDay/season/device/lens`(**仍不含像素、不含原始 GPS**)。浏览器预览验证:`ai.list()` = `["smartColor","caption"]`、按钮以 `--accent`(#C2632D)描边渲染、点击出 chip、点 chip 文案填入文字框并触发重绘、console 全程零报错。仍单文件 + 零外链;详见 `docs/roadmap-3.0.md` A3。
- **更早**(v3.0.0-beta.6):**拼图加文字 / 贴纸(预览上可拖动的叠加层,烤进 1500² 大图后照常走水印管线)**——6/4 冲刺第③项,南心确认范围 = 拼图上加文字 / emoji 贴纸(不做自由手绘)。在 beta.3 拼图基础上加「装饰层(deco)」:`collageDecos=[]`,每项 `{type:'text'|'sticker', x, y, content, size, color}`(`x,y` 中心点归一化、`size` 高度占比,故预览 600 与成图 1500 等比一致)。`drawCollageDecos`:text 先 `strokeText` 描边光晕(按字色明暗选黑 / 白 halo,复用 beta.5 的 `_hexToRgb`)再 `fillText`,sticker 直接 `fillText(emoji)`(系统彩色字形、零外部资源);选中画蓝色虚线框(`selIndex`,**confirm 传 -1 故成图不含框**)。`drawCollageInto` 扩参末尾叠 deco,两调用方分别传 `collageSelected` / `-1`。交互:点「＋ 文字」`prompt` 加字、点 `.deco-sticker` 加 emoji,**Pointer Events 拖动**(`#collagePreview` 加 `touch-action:none`、`getBoundingClientRect` 换算归一化 + 命中放宽 + `setPointerCapture`);选中行 `小一点 / 大一点 / 改文字 / 颜色 / 删除`(后两项对 sticker 隐藏)。换照片 / 取消 / 关 / confirm 各 `resetCollageDecos()`。**踩坑**:项目无全局 `.hidden`,新增 scoped `.collage-deco-edit.hidden` / `.deco-ctl.hidden`。浏览器预览(脚本注入两张纯色 PNG 走真实 change):贴纸 / 文字都画到预览、改色出 5641 绿像素、点「用这个拼」无异常切编辑器、**最终 1500×1680 合成图含 37690 绿像素**(证明 deco 烤进大图并穿过水印管线),全程零报错。仍单文件 + 零外链。
- **更早**(v3.0.0-beta.5):**自定义主题色(UI 强调色可换)**——设置→外观「主题」下方加「主题色」:6 个预设圆点 + 1 个彩虹取色盘(`<input type=color>`)。**核心手法:把选中色作为 inline style 写到 `document.body` 上**——inline 优先级高于任何选择器,故同时盖过 `:root` 与 `body[data-theme="dark"]` 两处默认 `--accent`;选「默认」`removeProperty` 还原。新增 `applyAccent()`(挂 `applyTheme()` 末尾):`--accent` 直接取选中色,`--accent-soft` 按明暗分别 `_mix`(浅色向奶白、深色向近黑)算出,结果贴近原手调默认。整套 UI 早已全程 `var(--accent)`/`var(--accent-soft)`,故只改这两变量即可全局换色。**画布水印只读 JS 端硬编码调色板、从不读 CSS 变量,故成图天然不受影响**。`accent` 入 `yinji.settings.v1` 持久化。浏览器预览验证:7 个色板 + 取色 input 在位、点海蓝→`body --accent`=#2E6F95 + soft 计算 + 持久化、点默认→inline 清空回落 #C2632D、深色下 soft 向近黑、**实测玻璃开关 slider 背景随选色赤陶↔胭脂实时变并能还原**、取色盘 input 持久化 #1f8a70、全程零报错。仍单文件 + 零外链。
- **更早**(v3.0.0-beta.4):**水印扩充 +6 款**(模板 36 → 42)——沿用 v2.7.1 / v2.9.1 / v2.10.0 的**零风险加法**:**只加调色 + 注册、零新绘制代码**。`SEASON` 加 4 款奶系柔色(`matcha` 抹茶 / `milkTea` 奶茶 / `lotusPink` 藕粉 / `oatCream` 燕麦,共用 `drawSeasonStyle`,浅底栏 + 细线 + 深字);`BRAND` 加 2 款胶片(`silver` 银盐 = 炭灰 + 银灰、`kodak` 柯达金 = espresso 暖棕 + 琥珀金,共用 `drawBrandStyle`,深底栏 + 撞色细条 + 撞色字)。两渲染器都只吃 `{bg,text,soft,accent}` 四键。**4 个改动点**:`SEASON`/`BRAND` 加调色、`TEMPLATES` 加 6 条、`DRAW_DISPATCH` 加 6 条映射、`CAT_IDS`(季节 +4 / 品牌 +2)。浏览器预览验证:更多模板面板渲染 **42** 个 `.template-item` 芯片、6 个新 id 逐个出图稳定 1200×956 **零报错**、采底栏左下角像素 6 款 bg **全部与设计 hex 精确相符**(EBF0DC / F1E6D6 / F2E7EA / F4EEDF / 1C1E20 / 1C1408)。仍单文件 + 零外链。
- **更早**(v3.0.0-beta.3):**拼图基础版**——空状态(原 `.drop` 包进 `#startView`)新增整宽「拼图」入口 `#collageEntry`;点它经隐藏 `<input multiple>` 一次选 2–4 张照片,在 `#collageOverlay` 里选固定格子布局(2 张左右 / 上下、3 张竖排三 / 横排三 / 1 大 2 小、4 张田字格 / 竖排四 / 横排四)、实时预览,点「用这个拼」合成一张 1500² 方图。**核心:`drawCollageInto` 合成到离屏 canvas → `state.bitmap=canvas`**,因 `getSize()` 读 `width||naturalWidth`、渲染一律 `drawImage`,故水印 / 滤镜 / 调色 / 导出**零改动全继承**(拼图无单一 EXIF → `state.exif=null`)。`collageRoundRect` 特意避开既有 `roundRectPath` 同名坑;开局先 `renderCollagePreview()` 落定 `collageLayoutId` 再渲染选项,保证首个布局即高亮。仍单文件 + 零外链。定位为「自定义拼贴」基础版,自由拖拽 / 调间距 / 边框留后续迭代。
- **更早**(v3.0.0-beta.2):**界面紧凑化 + 动效弹性(纯 CSS,零 JS / 零渲染改动)**——① 整体 UI 等比收紧(圆角 22→18px、头部 / 横幅 / 面板的 padding 与字号一圈缩小、图标按钮 44→40px),**横幅照片可一屏放下**(竖幅仍需轻滑);② 动效加「弹性」:新增 `--spring:cubic-bezier(.34,1.5,.56,1)` 缓动,入场(头部 / 面板 / 空状态)略微回弹、`yj-scaleIn` 改 3 段过冲、按钮 / 分段控件按压用同一 spring,手感更"弹";保留 `prefers-reduced-motion` 关动效。**只改外观尺寸与手感,功能 / 滤镜 / 导出全不变**;仍单文件 + 零外链。
- **更早**(v3.0.0-beta.1):**智能配色 A1(端上启发式,3.0 首个 AI 功能)**——调色盘面板顶部新增「✨ 帮我配色」按钮,点一下把当前照片缩到 48×48 采样(平均色 / 亮度 / 饱和),按规则算出一组 `state.toneAdj` 写进调色盘并立即重绘:偏冷回暖(回暖比压冷更积极、有封顶)、纠绿 / 品红偏色、灰则提饱和。**纯端上、不联网、不下载模型、照片不离开设备**;复用 v2.13.0 调色盘管线、**渲染零改动**,拨完属「未保存的微调」可复位 / 存为滤镜。已注册到 `window.Yinji.ai`(`available` 翻 true)、`getContext()` 增聚合 `colorStats`(非像素)。仍单文件 + 零外链;详见 `docs/roadmap-3.0.md` A1。
- **更早**(v2.14.0):**滤镜系统**——把 v2.12.0/v2.13.0 的「调色 + 调色盘」整体收编为**滤镜**(一档内置滤镜 = 一整套完成观感,调色盘成了「在某套滤镜上再手动微调」的图层)。① **「调色」统一改叫滤镜**;② **滤镜从 8 款扩到 20 款分 6 类**(胶片 / 黑白 / 暖调 / 冷调 / 清新 / 氛围),新增 12 款只往 `TONE_LIST`/`TONE_PARAMS` 加数据、仍走 `getSize()` 单点注入,零逐样式改动;③ **🎲 随机一套**——一键随机「模板 + 滤镜」,用户无需自己挑;④ **存为我的滤镜 + 分享 / 导入**——把当前调色盘效果存成自定义滤镜(本机 `yinji.filters.v1`),可生成离线分享码复制给朋友导入(与模板共用一套编解码,按 `type` 路由)。点内置滤镜会把调色盘归中性、清自定义选中;手动动了调色盘 / 滑杆即降级为「未保存的微调」可再存。**定位为 3.0 滤镜体系地基**,详见 `docs/roadmap-3.0.md` A1。仍单文件 + 零外链
- **更早**(v2.13.0):**调色盘**——把 v2.12.0 的一键色调(Beta)升级为**可手动微调**。在「调色」那排下方折叠一个「调色盘 ▾」,展开是 **iPhone 16 Pro 风格的 2D 调色盘**(横轴拖冷暖、纵轴拖品红–绿)+ **强度**滑杆(调色浓淡,0 = 原片)+ **饱和**滑杆 + **复位**。实现:在 8 款预设之上叠一层微调层 `state.toneAdj{temp,tint,sat,intensity}`,经 `effectiveParams()` 合并后**仍走 v2.12.0 的 `getSize()` 单点注入**,36 款样式 + 导出零改动全继承、随成图保存;强度用"原片 ↔ 调色结果"线性混合(K=0 即原图)。**定位为 3.0「智能配色」的手动版地基**(AI 将来填同一套 `toneAdj`),v2.14.0 把它收进滤镜模型。
- **更早**(v2.12.0,2.0 收官):① **调色 · 色调影调(Beta)**——样式条下方一排,8 款一键色调(原图 / 黑白 / 复古 / 暖阳 / 冷调 / 清新 / 高级灰 / 鲜艳),`getSize()` **单点注入** + 逐像素 `TONE_PARAMS`(非 `ctx.filter`,iOS 可靠),全样式 + 导出零改动继承(v2.13.0 在此之上加了调色盘微调,v2.14.0 收编为滤镜)。② **玻璃四周(glassFrame)** 新预设——四边磨砂 + 底栏文字,复用 `frostRect`,归「玻璃」分类。③ **「添加模板」→「选择模板」**——顶部大按钮改为打开「更多模板」面板挑样式(自建入口仍在面板内「＋ 新建我的模板」)
- **更早**(v2.11.0):**PWA「有新版本」提示**——上线新版后,已装到主屏的用户打开 App 会弹底部提示条 +「刷新」按钮,一键换新版。`sw.js` 去掉自动 `skipWaiting`、改 `message` 触发;页面监听 `updatefound`/`waiting`,`controllerchange` 重载(带 `swReloading` 闸防首装误刷新)。注意 SW "隔一版生效":本版上线后老用户需手动刷新一次拿到 v2.11.0,之后的新版才自动弹提示
- **更早**(v2.10.0):新增 **5 款预设**(中秋·月 / 雾蓝 / 暮山 / 黛紫 / 暗夜蓝,30 → 35 款),全部复用现有共享渲染器(`drawSeasonStyle` / `drawBrandStyle`),只加调色板再登记、零新绘制代码。中秋归「节日」、雾蓝 / 暮山 / 黛紫归「季节」、暗夜蓝归「深色」分类
- **更早**(v2.9.2):顶部常用栏**默认改空**(`segPins.v2`),首屏只剩一个**大「添加模板」按钮**、由用户在「更多模板」点「☆ 常用」自行钉选;取消"至少留一个常用模板"限制(可清到 0);右上角设置齿轮图标重画为**对称 8 齿**。纯 UI 微调,不改功能(老用户更新后顶部常用栏会清空一次,重新钉即可;自定义模板 / 照片不受影响)
- **独立设置页 + 一批新设置项**(v2.9.0):右上角齿轮打开,六段——外观 / 打开照片时 / 自动模式显示哪些信息 / 照片与导出 / 数据与隐私 / 关于;含默认行为、默认水印文字、自动 EXIF 六项各自可勾、导出画质 / 尺寸上限、一键重置(常用栏 / 模板 / 全部设置);本机持久(`yinji.settings.v1`)
- **深色模式**(v2.9.0):跟随系统 / 浅色 / 深色三选一,只换 App 界面外壳,**不影响水印成图**;跟随系统用 `matchMedia` 实时切换,同步状态栏 `theme-color`
- **新手引导**(v2.9.0):首次打开弹 4 张分步卡片(选图 / 挑样式 / 切文字 / 保存分享),看过本机记住不再弹,设置「关于」可重看
- **滤镜 + 调色盘**(v2.12.0 起,**v2.14.0 起统一叫「滤镜」**):样式条下方一排,**20 款滤镜分 6 类**(胶片 / 黑白 / 暖调 / 冷调 / 清新 / 氛围)+「我的」;**v2.13.0 起**其下加「调色盘 ▾」——iPhone 16 Pro 风格 2D 调色盘(横轴冷暖 / 纵轴品红–绿)+ 强度 + 饱和 + 复位,在某款滤镜上手动微调。**v2.14.0**:🎲 随机一套(模板 + 滤镜一起随机)+ 把调色盘效果「存为我的滤镜」+ 滤镜离线分享 / 导入(`yinji.filters.v1`)。与水印样式叠加、随成图保存;`getSize()` 单点注入 + 逐像素变换。AI 荐色留 3.0
- **54 种预设水印样式**(含季节【v3.0.0-beta.4 加奶系柔色:抹茶 / 奶茶 / 藕粉 / 燕麦;v3.1.1 加 海盐 / 蜜桃 / 青提;v3.5.0 加 晴空 / 香芋 / 陶玫】/ 节气 / 相机品牌【v3.0.0-beta.4 加银盐 / 柯达金】/ 深色【v3.1.1 加 酒红 / 黛青 / 绛紫;v3.5.0 加 摩卡 / 苔墨 / 暗玫】/ 玻璃质感【含 v2.12.0「玻璃四周」四边磨砂相框】/ 节日款)+ **38 款滤镜**(6 分类,v3.5.0 加 过期卷 / 琥珀 / 冰蓝 / 通透 / 暗夜 / 丝绒)+ **用户自定义模板 / 滤镜**(localStorage 持久)+ **模板 / 滤镜 / 风格包分享 / 导入**(离线分享码)+ **我的风格(风格包,v3.6.0 存/套用/删,v3.7.0 离线分享/导入,含自定义水印内嵌)**+ **批量加水印(v3.8.0,把当前这套效果逐张套到多张照片、零依赖 store-zip 打包下载,最多 30 张,纯本地)**+ **液态玻璃强度可调(v3.9.0,关/轻/标准/强,变量驱动,强档近 iOS 26/27 Liquid Glass)**
- **水印(原「模板」)在编辑器「水印」页内嵌分类浏览**(v3.0.0-beta.9:分类行 + chips,点一下即套用;beta.10 起用户可见文案统一叫「水印」)+ **自定义水印编辑器**(＋ 新建我的水印 / 导入 / 分享 / 删除)。⚠️ **已移除**:头部「更多模板」搜索按钮(beta.9 删入口,`#searchOverlay` 弹窗代码休眠未删)、主样式条「常用栏」(segPins,beta.10 **整套代码删除**,设置里「重置常用栏」项一并删)
- **导出**:保存到相册(Web Share「存储图像」)+ 画质三选一(标准 / 高清 / 无损 PNG)+ 保留拍摄信息(EXIF 回写)
- **复制到剪贴板 / 记住上次选择 / 长按看原图**(v2.8.0):成图一键复制(PNG,支持的浏览器才显示)+ 样式 / 模式 / 画质本机记住下次还原 + 编辑时长按预览看无水印原图。玻璃水印磨砂改 downscale-upscale 法(真机可见),顶部状态栏配色统一
- **HEIC 拍摄信息读取**(iPhone Safari,自写 ISOBMFF 解析)
- **PWA**:浏览器打开网址 → "安装到桌面 / 主屏",Win / 安卓 / iOS / macOS 全平台体验近原生,断网可用
- **液态玻璃 UI**:卡片 / 按钮 / 弹窗全部半透明 + backdrop-filter,入场动效,接近 iOS 26 质感,可一键关闭
- **App 图标**:液态玻璃质感(磨砂玻璃卡片 + 暖色渐变 + 高光描边,保留「照片 + 红条」概念),`icons/icon.svg` + `icons/icon-mask.svg`

**路线决策**:**统一走 PWA**,不做安卓 / iOS 原生打包(Capacitor 路线已弃用,v2.7.0 起相关文件全部移除)。Windows Tauri .exe 保留可选,优先级低。

**🚀 当前冲刺计划 → 务必先读 `docs/当前冲刺计划.md`**（项目管家维护，你负责执行，按天拆好了 6.3-6.5 三天的任务）。

**主要待办**:
- ✅ **v3.0.0 ~ v3.6.0 已部署上线**(2026-06-08~09,负责人"我都部署完了")。⏳ **v3.7.0~v3.17.0 共 11 个版本 + index.html 已就绪、自检通过,待负责人部署**(可一起上;需手动上传 `watermark.html` + `sw.js` + `CHANGELOG.md` + `icons/` + `manifest.webmanifest` + `index.html`)。**⚠️ 迁移中**:南心拟从 GitHub Pages 迁 Netlify(指南 `docs/Netlify部署流程.md`);百度统计 ID 已换成她新建站点 `f488ac0d27fb6dbe03580f6375784d1e`(随部署生效)。
- **3.0 主线推进中**:✅ **A1 智能配色已落地(v3.0.0-beta.1)**;✅ **UI 紧凑化 + 动效弹性已落地(v3.0.0-beta.2)**;✅ **拼图基础版已落地(v3.0.0-beta.3,2–4 张固定格子合成 → 当普通照片接入既有管线;已与南心确认设计:固定格子 / 2–4 张 / 整张加一个水印)**;✅ **水印扩充 +6 款已落地(v3.0.0-beta.4,奶系柔色 4 + 经典胶片 2,共用现成渲染器零新绘制代码,模板 36 → 42)**;✅ **自定义主题色已落地(v3.0.0-beta.5,设置→外观 6 预设 + 取色盘,inline `--accent` 覆盖,仅改 UI 外壳不动成图)**;✅ **拼图加文字 / 贴纸已落地(v3.0.0-beta.6,拼图预览上可拖动的文字 + emoji 贴纸,烤进大图后照常走水印管线;即 6/4「画图 × 自定义拼贴」南心确认范围)**;✅ **A3 文案灵感(端上规则版)已落地(v3.0.0-beta.7;v3.0.0-beta.9 又把词库扩到 15 池 + 每次换一批)**;✅ **编辑器「水印 / 调色」分页已落地(v3.0.0-beta.8)**;✅ **水印模板区做成「和滤镜一样」的内嵌分类浏览已落地(v3.0.0-beta.9,删头部「更多模板」按钮)**;✅ **清理三项(去灰色小字 #144 / 去常用栏小尾巴 #149 / 「模板」字样改「水印」#150)已落地(v3.0.0-beta.10)**;**南心 6/5 六点清单进度**:开头(去灰字)✅、①去小尾巴 ✅、⑥后半(模板→水印)✅;**待办** ②**联网 AI 文案 B1(#146)**——设置里默认关、**用户本人**自填 API key(密钥只存本机、Claude 绝不代填)、当次授权 + 明确告知「只发〈时间 / 季节 / 色调 / 设备〉文字、不发照片」、浏览器直连兼容 OpenAI 的服务、失败回落端上版、诚实标注 CORS 限制、④**增加更多滤镜预设(#151,扩 `TONE_LIST`/`TONE_PARAMS` 零新绘制)**、③**再加一个 AI 功能(#152,放最后)**;**⚠️ 阻塞待南心补图**:⑤「图四」(#153,要做的功能截图丢失、内容未知)、⑥前半「在图 5 位置加『导入滤镜』」(#154,滤镜区已有 `importFilterBtn`,需确认是否真缺 / 还是缓存看不到)。✅ **拼贴升为编辑器第三个标签已落地(v3.0.0-beta.13,水印 / 滤镜 / 拼贴三标签共存、互不影响、成图叠加,#145)**;✅ **联网 AI 文案 B1 已落地(v3.0.0-beta.14,#146,即上「待办②」,BYOK 默认关 + 当次授权 + 只发文字 + 失败回退端上)**;✅ **拼图入口改造已落地(v3.0.0-beta.15,#163——主入口多选 2–9 张直接拼图、删底部按钮、布局扩到 9 张;南心新三件事第 1 件)**;✅ **拼贴→贴纸已落地(v3.0.0-beta.16,#164——编辑器第三标签改「贴纸」,任意照片贴 emoji / 手绘 / 文字、可拖能改色能缩放,拼图能力保留;南心新三件事第 2 件)**;✅ **更多滤镜预设已落地(v3.0.0-beta.17,#151——滤镜 20 → 32 款,每分类 +2,纯数据扩 `TONE_LIST`/`TONE_PARAMS` 零新绘制)**;✅ **A4 场景识别荐样式已落地(v3.0.0-beta.18,#152——第三个端上 AI「✨ 帮我选样式」,粗判风景 / 夜景 / 美食 / 人像 / 日常 → 荐 3–4 款样式,纯端上启发式不联网;南心新三件事第 3 件 Task 3 收尾)**;🎉 **南心新三件事全部完成、v3.0.0 beta 全部开发收口**(三件事:①首页拼图 9 张 beta.15 ②拼贴→贴纸 beta.16 ③管家更新 = 更多滤镜 beta.17 + 再加一个 AI beta.18);**仍阻塞待南心补图**:管家 #153(图四)、#154(图五「导入滤镜」,滤镜区已有 `importFilterBtn` 疑重复)——两项缺截图不臆测;另排期 **A2 完整调色**、拼图「自由拼贴」进阶版。节奏 6.6 Beta / 6.9 正式,详见 `docs/roadmap-3.0.md` 与 `docs/当前冲刺计划.md`。
- 🎯 **4.0「成组与流程」——全部收口 🎉**：✅ 支柱 1 批量（v3.8.0）+ ✅ 支柱 2 分享目标（v3.12.0）+ ✅ 支柱 3 风格包（v3.6.0/v3.7.0）+ ✅ 风格包×批量联动（v3.13.0）。**全本地、零后端、不破红线**。仅剩：批量结果逐张存相册（待南心反馈）+ AI 支线穿插。**详细施工图见 `docs/roadmap-4.0.md`**。
- ⏸️ **真机验证:负责人本轮明确选择「真机不用做」**——以下真机项暂缓,部署后有空再逐项过(代码侧已在浏览器预览自检通过)
- (暂缓)真机验证 v3.0.0-beta.6:拼图选好照片后,「加字 / 贴纸」一排能点「＋ 文字」打字、点 emoji 贴纸贴上、手指拖动能挪位置、点中后「小一点 / 大一点 / 改文字 / 颜色 / 删除」都对(改文字 / 颜色只对文字有、贴纸没有)、文字描边在深色照片上看得清、点「用这个拼」后字 / 贴纸印进大图且照常加水印 / 滤镜 / 导出、断网下可用(纯本地)
- (暂缓)真机验证 v3.0.0-beta.5:设置→外观→主题色 6 个预设点一下 App 点缀色(按钮 / 开关 / 选中高亮)立刻变、彩虹取色盘能任意挑、点「赤陶(默认)」能还原、深色模式下底色不刺眼、导出的水印成图不受主题色影响
- (暂缓)真机验证 v3.0.0-beta.4:6 款新模板(抹茶 / 奶茶 / 藕粉 / 燕麦 / 银盐 / 柯达金)在「更多模板」对应分类(季节 / 品牌)能找到、点一下出图正常、底栏配色 / 字色 / 细线观感舒服、能钉到常用栏
- (暂缓)真机验证 v3.0.0-beta.3:空状态有「拼图」按钮、能一次选 2–4 张、各张数布局选项数对(2→2 / 3→3 / 4→3)、点布局预览实时变、「用这个拼」后进编辑器能照常加水印 / 滤镜 / 导出、「换照片」「取消」正常、断网下可用(纯本地合成)
- (暂缓)真机验证 v3.0.0-beta.2:界面在手机上是否更紧凑、横幅照片能否一屏放下(竖幅仍需轻滑)、入场 / 按钮按压的「弹性」回弹手感是否舒服、开了系统「减弱动态效果」时动效是否变稳(无障碍兜底)
- (暂缓)真机验证 v3.0.0-beta.1:调色盘里点「✨ 帮我配色」能按当前照片自动拨盘 + 立刻出效果、偏冷照片会回暖且不过度、点「复位调色盘」能还原、配完能「＋ 存为滤镜」、断网下照常可用(纯端上)
- (暂缓)真机验证 v2.14.0:滤镜分类条能横滑切换、各类 chip 数量对、点内置滤镜出图正常且会清掉调色盘微调、🎲随机一套能同时换模板 + 滤镜、调好效果点「＋ 存为滤镜」能存进「我的」、自定义滤镜能分享出码 + 别人导入能复现、删除能删掉
- (暂缓)真机验证 v2.13.0:「调色盘 ▾」能展开 / 折叠、2D 盘拖动调冷暖 / 色调即时反映到预览、强度拉到 0 回原片、饱和拉到底变灰、复位归中性、调好随成图保存
- (暂缓)真机验证 v2.12.0:调色 8 款一键切换出图正常 + 随成图保存、玻璃四周在「玻璃」分类能找到并出图、顶部按钮显示「选择模板」且点开是「更多模板」面板
- (暂缓)真机验证 v2.11.0:上线新版后,装到主屏的 App 能弹「有新版本」提示条、点「刷新」能换到新版、点「×」能关掉(注意"隔一版生效"——v2.11.0 这次本身可能要先手动刷一次,之后的新版才自动弹)
- (暂缓)真机验证 v2.10.0:中秋·月 / 雾蓝 / 暮山 / 黛紫 / 暗夜蓝 5 款都能正常出图、在「更多模板」对应分类(节日 / 季节 / 深色)能找到、能钉到常用栏
- 真机验证 v2.9.2:顶部默认只剩大「添加模板」按钮、在「更多模板」点「☆ 常用」钉选后顶部出现快捷栏、可一路取消钉选清空到 0(不再弹"至少留一个")、右上角设置齿轮图标对称
- 真机验证 v2.9.1:主样式条「＋ 添加模板」文案、设置「默认打开模板」下拉(用上次用的 / 指定样式两种)、导出文件名带日期 + 样式名、4 款新预设(佳能红 / 毕业季 / 莫兰迪 / 节气·青)、设置「AI · 即将推出」预告按钮弹 toast
- 真机验证 v2.9.0:齿轮设置页各项(深色模式跟随系统 / 浅 / 深、各默认行为、自动 EXIF 六项勾选、导出画质 / 尺寸上限、三个重置按钮)、新手引导首跑弹出 + 重看;以及历史项 v2.8.1 自挑常用栏 + 复制按钮位置、v2.8.0 玻璃磨砂 / 复制剪贴板 / 记住选择 / 长按看原图 / 状态栏配色、v2.7.1 新模板 + 新图标、v2.7.0 导出、v2.5.0 HEIC 读取、v2.6.0 模板导入导出
- **后续版本计划见 `docs/roadmap.md`**:3.0 之前还有若干 2.x 小步快跑(文案 / 易用性 / 更多预设打磨,不联网),3.0.0 起**以 AI 能力为主线**(接口已在 v2.9.1 预留——`window.Yinji.ai` + `getContext()` 只读快照;隐私铁律「照片绝不静默上传 / 端上优先 / 联网 AI 默认关 + 当次授权」见 `docs/tech.md` 6.11)
- 地点信息(GPS → 地名)
- 继续扩充预设(已加季节 / 节气 / 生日 / 纪念日 / 毕业季 / 莫兰迪 / 佳能红;可再加对联款等)

> 最新进展与待办**以 `devlog/更新日志-开发版.md` 最上面一节为准**;用户视角的版本历史看根目录 `CHANGELOG.md` 或 `devlog/更新日志-用户版.md`。
