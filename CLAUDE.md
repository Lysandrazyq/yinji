# 印迹 · 照片水印 —— 项目说明（CLAUDE.md）

> 本文件供 AI（Claude）在每次参与本项目开发时**最先阅读**。
> 它说明项目是什么、各标准文件在哪、以及必须遵守的工作规矩。

---

## 一、项目概况

「印迹」是一个运行在浏览器里的**照片水印网页 App**，主要面向 iPhone Safari，可"添加到主屏幕"当原生 App 用。

核心能力：给照片加一条**白框黑字**的底栏水印（类似 OPPO / 徕卡相机水印）。支持两种文字来源——手动输入、或自动读取照片 EXIF（设备、时间、光圈、快门、ISO、焦段），一键切换、实时预览。所有处理都在用户手机**本地**完成，照片不上传。

项目负责人是**非程序员**。沟通和交付都要照顾这一点（见第三节）。

---

## 二、项目结构与文件路径

```
yinji/
├── CLAUDE.md                 ← 本文件(工作总指引)
├── watermark.html            ← 应用本体(单文件,HTML+CSS+JS 全部内联)
├── manifest.webmanifest      ← PWA 元数据(App 名 / 图标 / 主题色)
├── sw.js                     ← PWA service worker(离线缓存)
├── package.json              ← Node 依赖清单(Tauri,可选)
├── icons/                    ← PWA 图标
│   ├── icon.svg              ← 普通图标
│   └── icon-mask.svg         ← maskable(安卓自适应)
├── docs/                     ← 标准文档(开工前按需阅读),每份 .md + .docx 双格式
│   ├── requirements.md / .docx       ← 需求、范围、路线图
│   ├── tech.md         / .docx       ← 技术选型、实现要点、平台接口、PWA
│   ├── design.md       / .docx       ← 视觉规范、配色、模板样式
│   ├── steps.md        / .docx       ← 开发 / 自检 / 部署 / 协作流程
│   ├── cross-platform.md / .docx     ← Win / PWA 打包指南(详细)
│   └── tauri.conf.example.json       ← Windows Tauri 配置范例
├── tools/
│   └── md2docx.py            ← 把 docs/*.md 重新生成同名 .docx(改完 md 后跑一下)
└── devlog/                   ← 更新日志(中文名双版)
    ├── 更新日志-用户版.md      ← 每天一条 App Store 风格总结(带日期)
    └── 更新日志-开发版.md      ← 按版本倒序的详细技术变更(带日期)
```

> `.docx` 是给负责人用 Word 打开看的,**由 .md 自动生成**,不要手动编辑。改完任何一份 .md,交付前都要跑 `python tools/md2docx.py` 重新生成(详见 `docs/steps.md` 3.5 节)。

> **跨平台**:核心永远是 `watermark.html`。PWA / Tauri 两种"装上"方式都只是套壳,不改核心(安卓 Capacitor 路线已弃用)。平台差异通过 `window.Yinji.platform.*` 接口注入,详见 `docs/cross-platform.md`。

> `.docx` 是给负责人用 Word 打开看的，**由 .md 自动生成**，不要手动编辑。改完任何一份 .md，交付前都要跑 `python tools/md2docx.py` 重新生成（详见 `docs/steps.md` 3.5 节）。

各文档用途速查：
- 改需求 / 加功能前 → 读 `docs/requirements.md`
- 动代码前 → 读 `docs/tech.md`
- 调样式 / 配色 / 排版前 → 读 `docs/design.md`
- 不确定怎么测、怎么发布、怎么和负责人协作 → 读 `docs/steps.md`

---

## 三、给 AI 的工作规矩（每次都要遵守）

1. **先读后做**：开工先读本文件，再读 `docs/` 下相关标准文件，确认改动符合既有需求与规范，不凭空发挥。

2. **面向非程序员沟通**：用大白话，不堆术语；涉及取舍先讲清楚利弊；**改动前先确认需求，得到同意再执行**。交付时给清楚的"怎么用"步骤。

3. **单文件 + 零外部依赖**（重要，踩过坑）：App 必须是一个自包含的 HTML 文件，**不引入任何需要联网才能加载的外部资源**（如外链字体、CDN 脚本）。字体一律用系统字体栈。原因：外链资源会在手机内置浏览器里阻塞渲染，导致白屏。

4. **隐私优先**：所有照片处理留在本地，绝不上传照片到任何服务器。

5. **改完必自检**（详见 `docs/steps.md`）：至少做到 JS 语法检查、EXIF 解析用样张验证、水印排版肉眼检查。

6. **写更新日志**（这就是"每天自动记录"的落地方式）：**每次完成一轮开发后**，更新 `devlog/` 下的两份日志——`更新日志-用户版.md`（给负责人看，当天加 / 补一条 App Store 风格的大白话总结，带日期）和 `更新日志-开发版.md`（给维护者看，在最上方加一节「日期 · 版本号」的详细技术变更）。这一步每次都要做，不要遗漏。

---

## 四、当前状态（摘要，详情见最新开发日志）

- **正式版网址**:https://lysandrazyq.github.io/yinji/watermark.html(GitHub Pages 部署,所有平台浏览器打开即用)
- **当前版本**:**v2.12.0**(**2.0 大版本收官**;详见 `CHANGELOG.md` 或 `devlog/更新日志-开发版.md`;3.0.0 起以 AI 能力为主线,见 `docs/roadmap.md`)
- **本轮新增**(v2.12.0,2.0 收官):① **调色 · 色调影调(Beta)**——样式条下方一排,8 款一键色调(原图 / 黑白 / 复古 / 暖阳 / 冷调 / 清新 / 高级灰 / 鲜艳)。架构关键:在 `getSize()` **单点注入**(把照片调色一次缓存进离屏 canvas、当 `bmp` 返回),35+ 款样式 + 导出**零改动全继承**、随成图保存;调色走**逐像素 `getImageData`/`TONE_PARAMS`**(非 `ctx.filter`——iOS canvas 对颜色滤镜无效,同玻璃磨砂的坑);完整版(强度滑杆 / 更多胶片色)留 3.0。② **玻璃四周(glassFrame)** 新预设——四边磨砂 + 底栏文字,复用 `frostRect`,归「玻璃」分类。③ **「添加模板」→「选择模板」**——顶部大按钮改为打开「更多模板」面板挑样式(自建入口仍在面板内「＋ 新建我的模板」)。仍单文件 + 零外链
- **上一版**(v2.11.0):**PWA「有新版本」提示**——上线新版后,已装到主屏的用户打开 App 会弹底部提示条 +「刷新」按钮,一键换新版。`sw.js` 去掉自动 `skipWaiting`、改 `message` 触发;页面监听 `updatefound`/`waiting`,`controllerchange` 重载(带 `swReloading` 闸防首装误刷新)。注意 SW "隔一版生效":本版上线后老用户需手动刷新一次拿到 v2.11.0,之后的新版才自动弹提示
- **更早**(v2.10.0):新增 **5 款预设**(中秋·月 / 雾蓝 / 暮山 / 黛紫 / 暗夜蓝,30 → 35 款),全部复用现有共享渲染器(`drawSeasonStyle` / `drawBrandStyle`),只加调色板再登记、零新绘制代码。中秋归「节日」、雾蓝 / 暮山 / 黛紫归「季节」、暗夜蓝归「深色」分类
- **更早**(v2.9.2):顶部常用栏**默认改空**(`segPins.v2`),首屏只剩一个**大「添加模板」按钮**、由用户在「更多模板」点「☆ 常用」自行钉选;取消"至少留一个常用模板"限制(可清到 0);右上角设置齿轮图标重画为**对称 8 齿**。纯 UI 微调,不改功能(老用户更新后顶部常用栏会清空一次,重新钉即可;自定义模板 / 照片不受影响)
- **独立设置页 + 一批新设置项**(v2.9.0):右上角齿轮打开,六段——外观 / 打开照片时 / 自动模式显示哪些信息 / 照片与导出 / 数据与隐私 / 关于;含默认行为、默认水印文字、自动 EXIF 六项各自可勾、导出画质 / 尺寸上限、一键重置(常用栏 / 模板 / 全部设置);本机持久(`yinji.settings.v1`)
- **深色模式**(v2.9.0):跟随系统 / 浅色 / 深色三选一,只换 App 界面外壳,**不影响水印成图**;跟随系统用 `matchMedia` 实时切换,同步状态栏 `theme-color`
- **新手引导**(v2.9.0):首次打开弹 4 张分步卡片(选图 / 挑样式 / 切文字 / 保存分享),看过本机记住不再弹,设置「关于」可重看
- **调色 · 色调影调(Beta,v2.12.0)**:样式条下方一排,8 款一键色调(原图 / 黑白 / 复古 / 暖阳 / 冷调 / 清新 / 高级灰 / 鲜艳),与水印样式叠加、随成图保存;`getSize()` 单点注入 + 逐像素变换。完整版留 3.0
- **36 种预设水印样式**(含季节 / 节气 / 相机品牌 / 玻璃质感【含 v2.12.0「玻璃四周」四边磨砂相框】/ 节日款)+ **用户自定义模板**(localStorage 持久)+ **模板分享 / 导入**(离线分享码)
- **模板搜索** + **自定义模板编辑器** + 主样式条大「选择模板」按钮(v2.12.0 由「添加模板」改名,点开「更多模板」面板挑样式)+ **主样式条常用模板用户可自挑**(v2.8.1,常用栏,localStorage 记住;v2.9.2 起**默认空**,首屏只剩大「选择模板」按钮,用户在「更多模板」点「☆ 常用」钉选后才出现快捷栏)
- **导出**:保存到相册(Web Share「存储图像」)+ 画质三选一(标准 / 高清 / 无损 PNG)+ 保留拍摄信息(EXIF 回写)
- **复制到剪贴板 / 记住上次选择 / 长按看原图**(v2.8.0):成图一键复制(PNG,支持的浏览器才显示)+ 样式 / 模式 / 画质本机记住下次还原 + 编辑时长按预览看无水印原图。玻璃水印磨砂改 downscale-upscale 法(真机可见),顶部状态栏配色统一
- **HEIC 拍摄信息读取**(iPhone Safari,自写 ISOBMFF 解析)
- **PWA**:浏览器打开网址 → "安装到桌面 / 主屏",Win / 安卓 / iOS / macOS 全平台体验近原生,断网可用
- **液态玻璃 UI**:卡片 / 按钮 / 弹窗全部半透明 + backdrop-filter,入场动效,接近 iOS 26 质感,可一键关闭
- **App 图标**:液态玻璃质感(磨砂玻璃卡片 + 暖色渐变 + 高光描边,保留「照片 + 红条」概念),`icons/icon.svg` + `icons/icon-mask.svg`

**路线决策**:**统一走 PWA**,不做安卓 / iOS 原生打包(Capacitor 路线已弃用,v2.7.0 起相关文件全部移除)。Windows Tauri .exe 保留可选,优先级低。

**主要待办**:
- **部署 v2.12.0 到 GitHub Pages**(上传 `watermark.html` + `sw.js` + `CHANGELOG.md` + `icons/`;v2.9.0 ~ v2.12.0 均未部署,线上仍是更早版本)——这是当前唯一的硬前置,需负责人手动上传
- ⏸️ **真机验证:负责人本轮明确选择「真机不用做」**——以下真机项暂缓,部署后有空再逐项过(代码侧已在浏览器预览自检通过)
- (暂缓)真机验证 v2.12.0:调色 8 款一键切换出图正常 + 随成图保存、玻璃四周在「玻璃」分类能找到并出图、顶部按钮显示「选择模板」且点开是「更多模板」面板
- (暂缓)真机验证 v2.11.0:上线新版后,装到主屏的 App 能弹「有新版本」提示条、点「刷新」能换到新版、点「×」能关掉(注意"隔一版生效"——v2.11.0 这次本身可能要先手动刷一次,之后的新版才自动弹)
- (暂缓)真机验证 v2.10.0:中秋·月 / 雾蓝 / 暮山 / 黛紫 / 暗夜蓝 5 款都能正常出图、在「更多模板」对应分类(节日 / 季节 / 深色)能找到、能钉到常用栏
- 真机验证 v2.9.2:顶部默认只剩大「添加模板」按钮、在「更多模板」点「☆ 常用」钉选后顶部出现快捷栏、可一路取消钉选清空到 0(不再弹"至少留一个")、右上角设置齿轮图标对称
- 真机验证 v2.9.1:主样式条「＋ 添加模板」文案、设置「默认打开模板」下拉(用上次用的 / 指定样式两种)、导出文件名带日期 + 样式名、4 款新预设(佳能红 / 毕业季 / 莫兰迪 / 节气·青)、设置「AI · 即将推出」预告按钮弹 toast
- 真机验证 v2.9.0:齿轮设置页各项(深色模式跟随系统 / 浅 / 深、各默认行为、自动 EXIF 六项勾选、导出画质 / 尺寸上限、三个重置按钮)、新手引导首跑弹出 + 重看;以及历史项 v2.8.1 自挑常用栏 + 复制按钮位置、v2.8.0 玻璃磨砂 / 复制剪贴板 / 记住选择 / 长按看原图 / 状态栏配色、v2.7.1 新模板 + 新图标、v2.7.0 导出、v2.5.0 HEIC 读取、v2.6.0 模板导入导出
- **后续版本计划见 `docs/roadmap.md`**:3.0 之前还有若干 2.x 小步快跑(文案 / 易用性 / 更多预设打磨,不联网),3.0.0 起**以 AI 能力为主线**(接口已在 v2.9.1 预留——`window.Yinji.ai` + `getContext()` 只读快照;隐私铁律「照片绝不静默上传 / 端上优先 / 联网 AI 默认关 + 当次授权」见 `docs/tech.md` 6.11)
- 地点信息(GPS → 地名)
- 继续扩充预设(已加季节 / 节气 / 生日 / 纪念日 / 毕业季 / 莫兰迪 / 佳能红;可再加对联款等)

> 最新进展与待办**以 `devlog/更新日志-开发版.md` 最上面一节为准**;用户视角的版本历史看根目录 `CHANGELOG.md` 或 `devlog/更新日志-用户版.md`。
